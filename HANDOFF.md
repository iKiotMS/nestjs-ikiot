# Session Handoff

Đọc file này trước khi làm tiếp — dành cho một phiên Claude Code mới (hoặc bạn) tiếp tục công việc migrate `iKiotMS-BE` (Node/Express/Mongoose) sang `iKiot-BE` (NestJS/Prisma/PostgreSQL) từ một máy khác. File này là **nhật ký theo ngày** (mục mới thêm ở cuối, không sửa lại mục cũ trừ khi nó sai) — còn `CLAUDE.md` mới là tài liệu kiến trúc "hiện trạng đúng nhất", luôn cập nhật theo mọi quyết định ở đây. Đọc `iKiot-BE/CLAUDE.md` và `iKiotMS-BE/CLAUDE.md` trước.

## Bối cảnh

`Code-trom/` là workspace chứa 2 project độc lập:
- **`iKiotMS-BE/`** — backend cũ (Express + MongoDB), nguồn sự thật cho toàn bộ business logic. Không sửa gì ở đây trừ `CLAUDE.md`.
- **`iKiot-BE/`** — backend mới (NestJS + Prisma + PostgreSQL), đang được build dần từ đây.

## 2 tài liệu tham chiếu đã xuất bản (Claude Artifacts)

Nếu đăng nhập cùng tài khoản Claude trên máy khác, 2 link này vẫn xem được (browse thêm tại claude.ai/code/artifacts nếu link đổi):

- **iKiotMS Postgres Migration** — https://claude.ai/code/artifact/bc9977f1-823d-41c3-a713-4ded56332db3 — bản đồ chuyển đổi từng field của 34 model Mongoose sang bảng Postgres/Prisma.
- **iKiotMS Feature Ledger** — https://claude.ai/code/artifact/53e9a4c4-4409-487a-9fe6-b3f5901d9114 — kiểm kê đầy đủ ~150 endpoint + business rule của 28 module trong `iKiotMS-BE`, cộng **56 vấn đề/bug/điểm mơ hồ** cần chốt trước khi port từng module. **Đọc mục "Vấn đề cần chốt" trong đó trước khi port bất kỳ module nghiệp vụ nào** — đặc biệt payroll (#27: bonus engine chưa từng chạy, phải xây mới) và leave-request (#25: mâu thuẫn quyền warehouse manager).

## Nhật ký theo ngày

### 2026-08-14 — RBAC + Auth nền tảng

1. **`prisma/schema.prisma`** — schema Postgres đầy đủ cho 33 entity + child/join table, convert từ Mongoose (xem comment đầu file schema).
2. **RBAC tuỳ biến theo tenant** — thay hệ 6-role cố định cũ bằng `User.systemRole` (`ADMIN | TENANT_OWNER | CUSTOMER | STAFF`) + `Role`/`RolePermission`/`PermissionCatalog`. Chi tiết đầy đủ: mục **"Authorization"** trong `iKiot-BE/CLAUDE.md`.
3. **3 module đầu tiên có guard thật**: `auth`, `roles`, `users`.
4. **Guard toàn cục**: `JwtAuthGuard` + `PermissionsGuard` gắn qua `APP_GUARD`.
5. Bắt và sửa 1 lỗ hổng có sẵn từ trước: `main.ts` chưa từng bật `ValidationPipe` toàn cục — đã bật.

Lúc này: chưa có Postgres thật để test, chỉ `pnpm run build`/`tsc --noEmit` pass. Git: 1 commit gốc, mọi việc trong session đang staged chưa commit.

### 2026-08-17 — Swagger, OTP/Firebase thật, seed admin, Audit, Subscription & Billing

1. **Swagger** — `@nestjs/swagger` + CLI plugin (tự sinh metadata từ `class-validator`, xem `nest-cli.json`), `/docs`, `@ApiTags` trên cả 35 controller lúc đó. Rule bắt buộc cập nhật Swagger cùng lúc sửa API đã thêm vào `CLAUDE.md`.
2. **OTP đăng ký thật** — `OtpService`/`EsmsService` port từ `otpService.js`/`esmsService.js`. Chưa có `ESMS_API_KEY` trong `.env` → code OTP log ra console server (đúng hành vi cũ). Lưu OTP **in-memory** (chưa Redis).
3. **Firebase login thật** — `FirebaseService` port từ `config/firebase.js`. `.env` thiếu `FIREBASE_PRIVATE_KEY` → endpoint trả 401 "chưa cấu hình" cho tới khi bạn thêm key đó.
4. **Seed tài khoản ADMIN**: SĐT `0000000000` / mật khẩu `password123`, `systemRole: ADMIN`, không thuộc tenant nào. **Đổi mật khẩu này trước khi deploy thật** — đây là credential cố định, ai đọc code cũng biết.
5. **Audit logging thật** — `AuditInterceptor` (global, port từ `auditMiddleware.js`) + `GET /admin/audit-logs`.
6. **Subscription & Billing thật** — port đầy đủ từ `SubscriptionService`/`SubscriptionController` cũ: `plans` (public + admin CRUD), `subscriptions` (free-trial/status/upgrade/renew/webhook SePay), `subscription-invoices`. Kèm theo vì Subscription phụ thuộc: `NotificationService.notify()`/`tenantOwners()` (ghi DB thật, **chưa có Socket.IO/push** — chỉ mới viết `Notification` row), `EmailService` (nodemailer thật), `SepaySubscriptionService` (tách riêng SePay cho subscription khỏi SePay cho order — đừng gộp 2 cái khi port order), cron job nhắc hết hạn hàng ngày (`@nestjs/schedule`), seed 5 gói TRIAL/PLUS/PLUS_YEARLY/PRO/PRO_YEARLY.
7. **Đã chạy migrate + seed + test thật trên Postgres local** (`docker-compose.yml` bạn tự thêm, `postgres:17` tại `localhost:5432`) — không chỉ compile suông: đăng nhập admin, xem audit log, đăng ký tenant mới bằng OTP dev-bypass, gán free-trial, nâng cấp gói (sinh QR SePay thật), giả lập webhook thanh toán → subscription chuyển `ACTIVE` đúng, admin bật/tắt gói, xác nhận non-admin bị 403. Tất cả pass.
8. Chi tiết kỹ thuật đầy đủ của tất cả mục trên: 2 mục mới trong `CLAUDE.md` — **"Audit logging"** và **"Subscription & billing"**, cộng mục **"Cross-cutting services ported so far"**.

Lúc này: `.env` đã có `DATABASE_URL` thật (Postgres local qua Docker) + hầu hết secret thật khác (SEPAY_*, MAIL_*, CLOUDINARY_*, GOOGLE_*, REDIS_URL, GEMINI_API_KEY) — chỉ thiếu `FIREBASE_PRIVATE_KEY`, `ESMS_API_KEY`/`ESMS_SECRET_KEY`.

**Cùng ngày, phần 2 — Socket.IO + Notification/Audit thật cho tenant + kiến trúc chống god-service:**

9. **`RealtimeGateway`** (`src/common/realtime/`, global) — Socket.IO thật, port từ `socketService.js` nhưng sửa 1 lỗ hổng bảo mật: bản cũ cho client tự `emit('join', room)` bất kỳ room nào kể cả `admin`; bản mới server tự join room (`user:<id>`, `tenant:<id>`, `admin`) dựa trên JWT lúc connect, client không tự chọn room được.
10. **`NotificationService.notify()`** giờ emit socket thật (trước đó chỉ ghi DB). Inbox tenant port đầy đủ: `GET /notifications`, `/unread-count`, `PATCH /read-all`, `PATCH /:id/read`, `DELETE /`, `DELETE /:id`, `POST`/`DELETE /device-token`.
11. **Audit mở rộng cho tenant** — trước chỉ log `ADMIN`, giờ log mọi actor trừ `CUSTOMER`. Thêm `GET /audit-logs` (owner-only, scope theo tenant) song song `GET /admin/audit-logs` (platform, không đổi).
12. **Kiến trúc chống god-service (yêu cầu rõ của bạn)** — tách nội dung notification + logic mô tả audit ra khỏi service dùng chung:
    - Notification: `src/modules/notifications/templates/*.templates.ts` (vd `subscription.templates.ts`) — service module khác gọi template, không hardcode title/description.
    - Audit: `src/common/audit/audit-descriptor.ts` (interface) + `src/modules/<domain>/<domain>.audit-template.ts` (vd `subscription.audit-template.ts`) — `AuditInterceptor` chỉ còn cơ chế chung + fallback, không còn if/else riêng theo route. Đăng ký descriptor mới qua factory `APP_INTERCEPTOR` trong `app.module.ts` (1 dòng `inject` mỗi module).
    - Rule đầy đủ đã ghi vào `CLAUDE.md` mục **"Notification & audit templates — no god services"**.
13. **2 bug thật bắt được lúc test (không phải suy đoán)**:
    - `AuditInterceptor` fallback tên actor hardcode literal `"ADMIN"` — sai với mọi role khác không có tên/email. Sửa: fallback về `phoneNumber` (đã thêm field này vào `AuthUser`).
    - `SubscriptionAuditTemplate.matches()` dùng `path.includes('/subscription/upgrade/')` — vô tình khớp luôn `/subscription/upgrade/initiate` (route hoàn toàn khác, của tenant tự nâng cấp) chứ không chỉ `/subscription/upgrade/:tenantId` (route admin). Sửa: thêm `&& !path.endsWith('/initiate')`.
14. **Test thật end-to-end** (không chỉ build): đăng nhập, connect Socket.IO thật bằng JWT (dùng `socket.io-client` cài tạm rồi gỡ), trigger nâng cấp gói + giả lập webhook trong lúc socket đang lắng nghe → nhận đúng event `notification` qua socket. Audit tenant/admin đều đúng actor + resource sau khi sửa 2 bug trên.

Lúc này: build/lint/typecheck sạch, đã test thật qua Postgres + Socket.IO local, đã dọn hết dependency/file tạm dùng để test.

### 2026-08-17, phần 3 — dọn dẹp (code review toàn repo)

Không thêm tính năng, chỉ dọn những thứ review phát hiện:

15. **Xoá `src/user/`** — 4 file **rỗng 0 byte** (`user.module.ts`, `user.controller.ts`, `user.service.ts`, `schemas/user.schema.ts`) sót lại từ scaffold Mongoose ngày 12/8, đã được commit vào git, không import ở đâu. Module user thật là `src/modules/users/`.
16. **`pnpm run lint` từ trước đến giờ vẫn FAIL — giờ mới sạch.** 55 lỗi: 54 lỗi `no-unsafe-assignment` do generator sinh `data: data as any` trong 27 service CRUD, + 1 import `Query` thừa. Hoá ra cast đó **hoàn toàn không cần** (chính `eslint --fix` tự gỡ và `tsc` vẫn pass) → bỏ hẳn `as any`, không thay bằng type khác. Đã sửa cả `scripts/generate-modules.js` để chạy lại generator không tái tạo lỗi, đồng thời cho nó emit sẵn `@ApiTags` và chỉ import `Query` khi model có `tenantId`.
17. **Comment TODO sai sự thật ở 27 controller** (`// TODO: apply JwtAuthGuard ... once auth/tenant are ported`) — auth đã port xong từ ngày 14/8 và `JwtAuthGuard` là global guard nên các route này **đã** được bảo vệ. Thay bằng comment đúng: đã có JWT guard, còn thiếu `@Permissions()` + tenant-scoping.
18. **`test:e2e` từ trước tới giờ chạy là vỡ** (chưa ai chạy). 3 nguyên nhân xếp chồng, đã fix hết trong `test/jest-e2e.json` + script: Prisma 7 `import()` WASM query compiler → cần `node --experimental-vm-modules`; client Prisma sinh ra dùng specifier kiểu ESM `./x.js` → cần `moduleNameMapper` bỏ đuôi `.js`; `firebase-admin` → `jwks-rsa` → `jose` (ESM-only) → stub bằng `test/jose.stub.js`. Giờ e2e boot nguyên `AppModule` thật với Postgres thật và **pass**.
19. **`GET /` "Hello World!"** của scaffold → đổi thành healthcheck `@Public()` trả `{status, uptime}` (global `JwtAuthGuard` sẽ 401 endpoint này nếu không đánh dấu `@Public`, mà healthcheck của load balancer thì không có token). Cập nhật luôn cả unit test và e2e test tương ứng.
20. **`void bootstrap()`** trong `main.ts` — dọn nốt warning `no-floating-promises` duy nhất còn lại.

Đã verify lại sau khi dọn: `tsc` sạch, `lint` sạch (lần đầu tiên), `build` OK, unit test pass, e2e pass (2/2), và smoke test thật trên server dev: healthcheck 200, `/notifications` không token → 401, login admin OK, và CRUD đầy đủ (create/update/delete) trên `brands` — một trong 27 service vừa bị sửa — đều đúng.

### 2026-08-17, phần 4 — tenant/actor lấy từ token, không nhận từ client

21. **`src/common/utils/tenant-scope.ts`** — 2 hàm quyết định tenant của mọi request: `resolveTenantScope(user, requested?)` cho **đọc** (ADMIN không nêu tenant → `undefined` = mọi tenant; người khác luôn là tenant của chính mình, gửi tenant khác → 403) và `requireTenantId(user, requested?)` cho **ghi** (ADMIN buộc phải nêu tenant, nếu không → 400).
22. **26 module CRUD sinh tự động**: `tenantId` không còn nằm trong query/body nữa mà lấy từ token. `?tenantId=` chỉ còn là **override riêng cho ADMIN**. Đây vừa là dọn API vừa là vá lỗ hổng thật: trước đó bất kỳ user đã đăng nhập nào cũng đọc/ghi được dữ liệu của tenant khác chỉ bằng cách đổi query string.
23. **Trường "ai làm" cũng tự điền từ token**: `createdById` (CashFlow, Paysheet, PromotionLog, StockMovementRequest, WorkingSchedule) và `userId` ở những model mà nó thật sự nghĩa là người thao tác (Order, Ticket, LeaveRequest, AIChatHistory) — đã đối chiếu với controller cũ bên `iKiotMS-BE` để xác nhận. **Cố ý KHÔNG đụng** `Attendance.userId` và `Payslip.userId`: ở 2 model này `userId` là *nhân viên được chấm công / được tính lương*, quản lý nhập hộ người khác (xem `CreateManualAttendanceDTO.js` bên code cũ) — tự điền vào là sai nghiệp vụ.
24. **`findOne`/`update`/`remove` giờ mới thật sự scope theo tenant.** Trước đây chỉ `findAll` lọc, còn 3 cái kia tìm theo `id` không → đọc/sửa/xoá xuyên tenant được. Truy cập nhầm tenant trả **404 chứ không phải 403** (không để lộ việc bản ghi có tồn tại hay không). Dùng `findFirst` + `NotFoundException` tự ném, vì lỗi `findFirstOrThrow` của Prisma không phải `HttpException` nên Nest trả 500 — cái này phát hiện lúc test thật, không phải khi đọc code.
25. **Các module đã port thật** (`users`, `roles`, `audit-logs`, `subscriptions`, `subscription-invoices`): thay `user.tenantId!` bằng `requireTenantId(user)`. Non-null assertion đang che một bug thật — ADMIN có `tenantId = null` nên `GET /users` bằng token ADMIN trước đây trả `[]` im lặng, giờ báo 400 rõ ràng.
26. **`scripts/generate-modules.js`** thêm `PORTED_MODELS` — chạy lại generator trước đây sẽ **ghi đè và phá sạch** 9 module đã port bằng tay (User/Role/Plan/Subscription/AuditLog/Notification...). Giờ nó bỏ qua các model đó.

Test thật (tạo tenant B + owner B tạm rồi xoá): tạo brand không truyền tenantId → đúng tenant từng owner; A không thấy dữ liệu B; A đọc/sửa/xoá bản ghi của B → 404; A truyền `?tenantId=` của B → 403; ADMIN list → thấy cả 2 tenant, `?tenantId=` → lọc đúng; ADMIN tạo không nêu tenant → 400, có nêu → 201; Ticket/CashFlow tự điền đúng `userId`/`createdById`; nhét `tenantId` của B vào body → vẫn lưu vào tenant của A. Đã xoá sạch fixture và dữ liệu test sau khi chạy.

### 2026-08-17, phần 5 — `@Permissions()` cho toàn bộ 27 module CRUD

27. **Gắn `@Permissions()` cho cả 27 module** (5 route mỗi module = 100 cặp resource:action đang dùng). Map module → resource nằm ở `RESOURCE` trong `scripts/generate-modules.js`; generator **dừng hẳn** nếu có model chưa khai báo resource. Một số module cố ý **dùng chung** resource thay vì tự đẻ ra cái mới: `ProductItem` → `products` (biến thể sản phẩm), `ShiftTemplate` → `schedules`, `PromotionLog` → `promotions`.
28. **Phát hiện quan trọng khi đối chiếu code cũ**: `permissions.json` cũ có khai báo quyền, nhưng **route thật thì không gọi `authorize()`** — `/customers`, `/tickets`, `/products` chỉ có `verifyJwt`, ai đăng nhập cũng vào được. Vì vậy catalog gốc thiếu hẳn resource cho 4 module. Đã thêm mới: `customers`, `tickets`, `cash_flows`, `ai_chat`; và bổ sung action còn thiếu vào 7 resource sẵn có (`orders:delete`, `inventory:create/delete`, `attendances:delete`, `payrollSettings:delete`, `stock_movement:delete`, `cash_drawers:create/update/delete`, `payslips:create/read/update/delete`). Catalog: **121 → 150 cặp / 26 → 30 resource**. Mỗi chỗ thêm đều có comment giải thích ngay tại `CATALOG`.
29. **`scripts/check-permissions.js`** — script mới, đối chiếu mọi `@Permissions()` trong source với `CATALOG` trong seed. Cần thiết vì TypeScript không bắt được lỗi này: một cặp không có trong catalog thì **không role nào cấp được**, route âm thầm biến thành chỉ ADMIN/TENANT_OWNER vào được — kiểu bug rất lâu sau mới lộ. Chạy sau mỗi lần sửa decorator hoặc sửa seed. Nó cũng liệt kê các cặp có trong catalog mà chưa route nào dùng (hiện 50 — bình thường, vì các endpoint giàu hơn như `/orders/:id/pay-offline`, `/leave-requests/:id/approve` chưa port).
30. **Lưu ý đã ghi vào code + CLAUDE.md**: `@Permissions()` **không chặn được TENANT_OWNER** (họ short-circuit guard). Module `tenants` sinh tự động là resource cấp platform nên thật ra phải dùng `AdminOnlyGuard` như `/admin/plans`, chưa làm.

Test thật: tạo role "Thu ngan test" chỉ có `brands:read` + `orders:create`, gán cho 1 STAFF mới → `GET /brands` 200; POST/PATCH/DELETE `/brands` và toàn bộ `/payslips` `/products` `/customers` `/cash-flows` `/tickets` `/ai-chat-histories` `/inventories` `/orders` đều 403 kèm message rõ (`Missing permission payslips:read`). TENANT_OWNER và ADMIN vẫn vào hết. **Quan trọng nhất**: owner thêm `payslips:read` vào role → STAFF dùng **token cũ** gọi lại ngay được 200; thu hồi → 403 lại ngay. Đúng như thiết kế fetch quyền từ DB mỗi request (không cache trong JWT). Đã xoá sạch role/user test sau khi chạy.

### 2026-08-19 — port thật 5 module org/reference data (branches, warehouses, suppliers, brands, categories)

31. **Đổi schema + migration viết tay** `20260819043000_warehouse_contact_and_plan_max_warehouses`. `prisma migrate dev` **từ chối tự sinh** vì `plans.max_warehouses` là cột NOT NULL mà bảng đã có 5 dòng — nên migration này viết tay: thêm cột với `DEFAULT -1` rồi `DROP DEFAULT` ngay sau đó (gói mới bắt buộc phải khai hạn mức, giống `max_branches`). Ba thay đổi: `warehouses.phone_number`/`email` (kho giờ có thông tin liên hệ y như chi nhánh), `plans.max_warehouses`, `subscriptions.quota_snapshot_max_warehouses`. Seed 5 gói: Trial 1 kho / Plus 2 / Pro không giới hạn. **Lưu ý**: thêm một quota mới luôn là 4 chỗ sửa — `Plan`, `Subscription.quotaSnapshot*`, cả 3 chỗ ghi snapshot trong `subscriptions.service.ts`, và `prisma/seed.ts`.
32. **`SubscriptionService.requireActiveSubscription()` + `assertQuota()`** — thay cho middleware `requireActiveSubscription` của bản cũ. Luật hết hạn lazy (TRIAL/ACTIVE/PAST_DUE→EXPIRED) được gom vào một private `settleSubscription()` dùng chung với `checkTrialStatus()`, thay vì bản cũ chép luật ở 2 nơi. PAST_DUE vẫn cho qua (đúng ý nghĩa grace period).
33. **3 lỗi thật trong code sinh tự động của 5 module này** (không phải thiếu tính năng — là bug):
    - `remove()` là `prisma.delete()` **xoá cứng**. Bản cũ soft-delete branch/warehouse; xoá cứng một branch có users/orders/inventory/cash_flows trỏ vào sẽ nổ FK → 500.
    - `Supplier.outstandingDebt` **nằm trong Create/Update DTO**, client PATCH là sửa được công nợ. Bản cũ chặn phòng thủ trong service (`delete updateData.outstandingDebt`); giờ bỏ hẳn khỏi DTO.
    - `status` là field **bắt buộc** trong `CreateBranchDto`/`CreateWarehouseDto` → tạo được chi nhánh `status: "DELETED"` ngay từ đầu.
34. **`PATCH /:id/manager` đổi ngữ nghĩa** — bản cũ lật `User.role` giữa `STAFF` ↔ `BRANCH_MANAGER`/`WAREHOUSE_MANAGER`; hai role đó **không còn tồn tại** sau RBAC redesign. Giờ chỉ ghi `Branch.managerId`/`Warehouse.managerId`, **không đụng `roleId`** của nhân viên — quản lý được làm gì là do tenant tự gán Role. Ràng buộc: nhân viên phải ACTIVE, cùng tenant, và **chưa trực thuộc nơi làm việc khác** (bổ nhiệm không được âm thầm điều chuyển người).
35. **Kho giờ được đối xử y như chi nhánh** (theo yêu cầu "có nhiều warehouse"): phân trang/tìm kiếm/soft delete/quota gói/quy tắc bổ nhiệm chặt như branch. Bản cũ cho phép lấy **bất kỳ** nhân viên nào trong tenant làm quản lý kho — chỉ an toàn khi mỗi tenant có đúng 1 kho.
36. **Supplier giữ nguyên tính năng**: `POST /suppliers/:id/payments` (`suppliers:pay_debt`) trừ công nợ + ghi `CashFlow` EXPENSE + thông báo cho chủ cửa hàng, trong 1 transaction. Dùng `updateMany` có điều kiện `outstandingDebt >= amount` thay vì đọc-kiểm-ghi, để 2 lần thanh toán đồng thời không cùng lọt qua và ghi âm công nợ. Trả **200** (không phải 201 mặc định của Nest POST) cho khớp API cũ.
37. **Categories** giữ đủ: `GET /categories/tree` (phải khai **trước** `@Get(':id')`), breadcrumbs trong detail, chống vòng lặp `parentId` (tự làm cha / lấy hậu duệ làm cha), chặn xoá khi còn con hoặc còn sản phẩm. **Brands/categories giờ scope theo tenant** — bản cũ để global và **không có `authorize()` nào**, ai đăng nhập cũng sửa/xoá được thương hiệu của cả hệ thống.
38. **Dùng chung mới**: `common/dto/pagination-query.dto.ts` + `common/utils/pagination.ts` (`{data, pagination}` — đúng shape bản cũ), `common/dto/attendance-location.dto.ts` (map giữa 4 cột phẳng và object `attendanceTakingLocation` lồng của API cũ), `common/constants/location-status.ts`, `common/constants/payment-method.ts`.
39. Đã thêm `Branch`/`Warehouse`/`Supplier`/`Brand`/`Category` vào **`PORTED_MODELS`** — không thêm là lần chạy generator kế tiếp xoá sạch.

Test thật (2 tenant, 96 assertion, pass hết — script ở scratchpad, không commit): quota gói chặn đúng (Trial 2 chi nhánh / 1 kho, message rõ ràng); soft delete giữ nguyên dòng trong DB và biến mất khỏi list nhưng vẫn xem được bằng `?status=DELETED`; xoá chi nhánh/kho còn nhân viên → 400; cross-tenant đọc/sửa/xoá đều **404** (không phải 403); STAFF chỉ có `branches:read` → list 200, tạo/bổ nhiệm/xem supplier đều 403; bổ nhiệm quản lý không đổi `systemRole` lẫn `roleId`; nhân viên đang thuộc chi nhánh không bị kéo sang kho; trả nợ NCC 400k/1tr → còn 600k + đúng 1 `CashFlow` `SUP...` với `created_by` lấy từ token, trả vượt → 400 và **rollback sạch** (công nợ không đổi, không sinh cash flow); trial hết hạn → `settleSubscription` tự chuyển EXPIRED, `POST /branches` 403 nhưng `GET /branches` vẫn 200 (đúng bản cũ: chỉ route tạo mới bị chặn). Đã xoá sạch 2 tenant test khỏi DB sau khi chạy.

`tsc` · `pnpm run lint` · `node scripts/check-permissions.js` (không phải sửa seed — catalog đã đủ) · `pnpm run build` · unit 1/1 · e2e 2/2 đều sạch.

### 2026-08-20 — review toàn bộ code đã port rồi sửa hết những gì review chỉ ra

Không thêm module mới. Đây là đợt dọn: 5 lỗ hổng/thiếu sót thật, 3 nhóm trùng lặp, 8 chỗ khó đọc.

**A. Lỗ hổng & thiếu sót thật**

40. **4 route subscription của tenant trước đây không kiểm quyền gì cả** — `/subscription/free-trial`, `/subscription/status`, `/subscription/upgrade/initiate`, `/subscription/renew/initiate` chỉ có `JwtAuthGuard` toàn cục, không có `@Permissions()`. Nghĩa là **bất kỳ STAFF nào** cũng tự kích hoạt trial hoặc mở hoá đơn nâng cấp được. Giờ: `subscriptions:manage` cho 3 route ghi, `subscriptions:read` cho route status (TENANT_OWNER vẫn qua tự động vì short-circuit guard). Catalog đã có sẵn 3 action này, không phải sửa seed. Route `/subscription/upgrade/:tenantId` vẫn `AdminOnlyGuard` như cũ.
41. **Bật CORS + helmet trong `main.ts`** — cả hai đều chưa có, trong khi bản cũ `iKiotMS-BE` có đủ; frontend khác origin sẽ bị chặn ngay. CSP của helmet dùng bộ directive Nest khuyến nghị cho Swagger, vì CSP mặc định chặn luôn `/docs`. Origin đọc từ `CORS_ORIGIN` (danh sách ngăn bởi dấu phẩy; bỏ trống = cho mọi origin — **chỉ dùng khi dev**).
42. **`AllExceptionsFilter`** (`src/common/filters/`, đăng ký cả `APP_FILTER` lẫn `useGlobalFilters`) — trước đây mọi lỗi Prisma đều rơi ra **500 trống**. Giờ P2002→409, P2003/P2000/P2011/P2014→400, P2025→404, còn lại log stack rồi trả 500 chung chung (không rò message nội bộ). **`HttpException` được cho đi thẳng, không đụng vào** — body `{statusCode, message: string[], error}` của ValidationPipe là thứ frontend đang parse. *Đây là một nửa của mục "response envelope" trong danh sách còn nợ; nửa còn lại (interceptor bọc response) vẫn chưa làm.* — **cập nhật 26/8 phần 5 (mục 145): đã làm nốt**, và filter này giờ cũng trả `{success:false, ...}`; lỗi ValidationPipe chuyển danh sách theo field xuống `errors`.
43. **28 file `update-*.dto.ts` import `PartialType` sai chỗ** — từ `@nestjs/mapped-types` thay vì `@nestjs/swagger`. Validation vẫn đúng, nhưng plugin Swagger không thấy field kế thừa: `PATCH /branches/:id` trên `/docs` chỉ hiện mỗi `status`. Đã đổi hết + sửa **template trong `scripts/generate-modules.js`** (không sửa template thì lần chạy generator sau tái tạo lại lỗi). Gỡ luôn `@nestjs/mapped-types` khỏi dependencies.
44. **IP trong audit log trước đây client tự đặt được** — `AuditInterceptor` đọc thẳng header `x-forwarded-for` mà app chưa hề bật `trust proxy`. Giờ đọc `request.ip` (Express tự suy ra, và chỉ tin header khi `trust proxy` được cấu hình), `main.ts` set `trust proxy` từ `TRUST_PROXY` (số hop; bỏ trống = không có proxy). Hai biến mới `CORS_ORIGIN`/`TRUST_PROXY` đã ghi chú trong `.env`.

**B. Trùng lặp đã gom**

45. **`branches.service.ts` và `warehouses.service.ts` giống nhau ~90%** (223 và 211 dòng) → `LocationService` (`src/modules/locations/location.service.ts`) giữ toàn bộ logic, hai service còn **~45 dòng** mỗi cái: một `LocationConfig` (delegate Prisma, quota, cột `User` ghi nơi làm việc, và toàn bộ chuỗi tiếng Việt) + một wrapper `assignManager` để giữ nguyên key `branchId`/`warehouseId` trong response. `src/modules/locations/` **không phải Nest module**, không có route `/locations`. Delegate được khai kiểu structural (`LocationDelegate`) — `prisma.branch` và `prisma.warehouse` **thoả mãn nó nguyên trạng, không cast**; đổi lại `include` bắt buộc ở mọi lệnh đọc và các method trả `Prisma.PrismaPromise` để dùng được trong `$transaction`. `QueryBranchDto`/`QueryWarehouseDto` giờ extend `QueryLocationDto`. Lý do làm việc này: cặp này **đã lệch nhau một lần rồi** ở bản cũ (branch chặn điều chuyển nhân viên, warehouse thì không).
46. **Luật chuyển trạng thái subscription tồn tại 2 bản** — `settleSubscription()` (lazy) và `runSubscriptionStatusCheck()` (cron) mỗi bên tự viết lại, và **đã lệch**: bản lazy cho ACTIVE nhảy thẳng EXPIRED khi quá grace period, bản cron bắt buộc đi qua PAST_DUE. Giờ chỉ còn **một hàm thuần** `nextSubscriptionStatus(subscription, now)` trong `subscription-status.ts`, có unit test (`subscription-status.spec.ts`). Cron đổi từ 3 `updateMany` viết tay sang: nạp candidate → áp hàm chung → gom theo trạng thái đích → update theo lô.
47. **Hằng số lặp** — `INACTIVE_STATUSES` khai 2 lần y hệt (`auth.service.ts`, `jwt.strategy.ts`) và ~18 chuỗi `'ACTIVE'`/`'DELETED'` rải rác → `src/common/constants/user-status.ts` (`UserStatus`, `INACTIVE_USER_STATUSES`, `SETTABLE_USER_STATUSES`), song song với `location-status.ts` đã có. `MANAGER_SELECT` khai 2 lần → về `locations/location.types.ts`.

**C. Chỗ khó đọc đã viết lại**

48. **`withNestedAttendanceLocation`** — bỏ `Omit<T, typeof KEYS[number]>` + vòng `delete` + 2 lần cast, còn một destructure 4 field rồi spread phần còn lại; TypeScript tự suy ra kiểu trả về. Phải bật `ignoreRestSiblings: true` cho `no-unused-vars` trong `eslint.config.mjs` — chính vì thiếu option đó mà hàm này (và `toPublicUser`) mới bị đẩy sang cách viết cast/delete.
49. **`AuditInterceptor.resolveActor`** — nhánh login trước đây cast từng field khỏi response body (`u.systemRole as string`...), `AuthService` đổi shape là audit ghi sai mà không ai biết. Giờ có `AuditableLoginResponse` (`common/types/login-response.type.ts`), `AuthService.login`/`firebaseLogin` khai bằng `satisfies` → đổi shape là **gãy build**. `toPublicUser` cũng đổi từ `Record<string, unknown>` sang generic giữ nguyên kiểu.
50. **`handleSepayWebhook` không còn biết gì về HTTP** — trước trả `{httpStatus, body}` để controller `res.status()` bằng tay; đó là service **duy nhất** trong repo biết đến HTTP, junior đọc rồi bắt chước là hỏng convention. Giờ: `@HttpCode(200)` trên route, service trả `{success, message?}`, sai API key thì ném `UnauthorizedException` (401). Hành vi với SePay không đổi: mọi trường hợp khác vẫn 200 để nó không retry vào bug của mình.
51. **Tách `subscriptions.service.ts` (525 dòng, gánh 4 việc)** → `SubscriptionService` giữ *trạng thái* (trial, settle, status, requireActive, quota, adminUpgrade — chỉ phụ thuộc Prisma) và `SubscriptionBillingService` giữ *thu tiền* (invoice, QR, webhook). Hai bên gần như không gọi nhau. Chỉ `SubscriptionService` được export ra ngoài module.
52. **`assertQuota`** — tham số `quota` từ union 2 giá trị viết tay đổi thành `QuotaField` suy ra từ schema, thêm quota mới là dùng được ngay. **Sửa luôn một bug**: `max <= 0` coi `0` là "không giới hạn", nên gói cấu hình `maxBranches: 0` (ý là cấm tạo) lại thành tạo thoải mái. Giờ `null` hoặc **số âm** mới là không giới hạn. Ba chỗ ghi snapshot gom về `quotaSnapshotOf(plan)` — thêm quota giờ là 3 chỗ sửa, không phải 4 (mục 31 cũ nói 4).
53. **`daysBetween` dùng `Math.ceil` trên millisecond** → `daysLeft` đổi theo *giờ trong ngày* gọi API (10 ngày 1 tiếng → 11). Thay bằng `wholeDaysBetween` đếm giữa hai mốc nửa đêm UTC, có test.
54. **Descriptor audit không còn phải khai ở `app.module.ts`** — trước là `useFactory` + `inject` liệt kê tay, thêm template mới phải nhớ sửa **2 mảng**, quên thì vẫn compile và âm thầm rơi về mô tả generic. Giờ `@AuditTemplate()` (`DiscoveryService.createDecorator()`), `AuditInterceptor.onModuleInit()` tự quét; module domain chỉ cần khai provider của mình. `AppModule` import thêm `DiscoveryModule`.
55. **Categories: bỏ N+1** — breadcrumb trong `findOne` và `assertNoCycle` mỗi cái query 1 lần/tầng (tối đa 20 query cho 1 request). Giờ nạp cả cây 1 query rồi đi bộ trong bộ nhớ, giống `findTree` vốn đã làm vậy.
56. **Cron: bỏ N+1** — `sendExpiryReminders` tìm chủ cửa hàng bằng 1 `findFirst` cho **mỗi** subscription; giờ 1 query cho cả lô.

57. **Thêm mục `## Coding rules` vào `CLAUDE.md`** (ngay trước `## Architecture`, 25 rule chia 6 nhóm: bảo mật/đúng sai, một-bản-duy-nhất-của-mỗi-luật, phân tầng, kiểu, truy vấn, và checklist trước khi coi là xong). Mỗi rule được viết ra vì **đã có chỗ vi phạm và vi phạm đó im lặng** — phần lớn chính là 17 mục 40–56 ở trên; mỗi rule có ngoặc vuông trỏ về mục Architecture giải thích lý do. Mục đích là lần sau không phải review lại mới phát hiện.

**Đã cân nhắc và cố ý KHÔNG làm** (nêu ra để lần sau khỏi tưởng là bỏ sót):
- `CreateOrderDto` vẫn nhận `status`/`grandTotal`/`customerPay`/`change`/`discountValue` từ client. Khoá lại bây giờ là làm hỏng endpoint: module orders chưa port, **không có gì tính được các số đó** để thay thế. Phải làm cùng lúc với port thật module orders. Các DTO sinh tự động khác nhiều khả năng cùng vấn đề.
- Đổi `tenantId?: string` thành kiểu tường minh (`TenantScope = {all: true} | {tenantId: string}`) để compiler bắt lỗi quên check — đúng nhưng đụng vào 22 module sinh tự động **và** template generator; churn lớn trên code sẽ được sinh lại. Để lại khi port thật từng module.

**Kiểm chứng**: `tsc --noEmit` sạch · `pnpm run lint` sạch · `pnpm run build` sạch · `node scripts/check-permissions.js` OK (3 cặp `subscriptions:*` mới đều có sẵn trong catalog, không phải sửa seed) · unit **8/8** (thêm `subscription-status.spec.ts`) · thêm `test/di-check.e2e-spec.ts` **2/2** — spec này chỉ `.compile()` DI graph, **không cần database**, và kiểm rằng `@AuditTemplate()` thật sự được discovery tìm thấy (nếu không, audit chỉ âm thầm rơi về mô tả generic chứ không lỗi). **Chưa chạy được e2e đầy đủ**: Docker Desktop không chạy trên máy lúc làm, nên các e2e cần Postgres chưa xác minh — chạy lại `docker compose up -d && pnpm run test:e2e` trước khi commit.

### 2026-08-25 — port thật module Product + Inventory

Chủ dự án chốt rút gọn thứ tự build: làm trước nhóm **2 (Staff), 4 (Sản phẩm/kho), 5 (Bán hàng), 6 (Còn lại)**; nhóm 1 (cấu hình nhân sự) và 3 (chấm công/nghỉ phép) để sau. Bắt đầu bằng product + inventory.

**Route đã có** (11 + 4, khớp bản cũ trừ 2 chỗ ghi ở dưới)

58. `products`: `POST/GET/PATCH/DELETE /products`, `GET /products/:id`, `GET /products/search` (tra cứu kiểu POS), `GET /products/items` (danh sách phẳng cho dropdown), `POST /products/:productId/items`, `PATCH|DELETE /products/items/:itemId`, `POST /products/items/:itemId/suppliers`. `inventories`: `GET /inventory`, `PATCH /inventory/:id/min-stock`, `POST /inventory`, `DELETE /inventory/:id` — giữ path `/inventory` số ít như API cũ và như tên resource trong catalog.
59. **Bẫy thứ tự route**: `GET /products/items` và `/products/search` phải khai **trên** `GET /products/:id`, nếu không router hiểu chúng là product id. Đúng cái bẫy mà module Express cũ đã ghi comment cảnh báo.

**Lỗ hổng bản cũ đã vá**

60. **Toàn bộ 11 route product của bản cũ chạy trên `verifyJwt` trần, không có `authorize()` nào.** Bất kỳ tài khoản nào đăng nhập được — kể cả CUSTOMER — đều tạo/sửa/ngừng kinh doanh được cả catalogue của cửa hàng. Resource `products` **đã có sẵn** trong `permissions.json` từ đầu, chỉ là không ai gắn vào route. Giờ đủ `products:create/read/update/delete`.
61. **`createProduct` bản cũ bọc toàn bộ thân hàm trong `if (subscription)`** — tenant không có gói thì hàm trả `undefined` và API trả **HTTP 200**: không tạo gì, không báo gì. Giờ đi qua `assertQuota('quotaSnapshotMaxProducts')`, đếm sản phẩm khác DISCONTINUED; không có gói là 403 rõ ràng.
62. **`categoryName` không còn nhận từ client.** Nó là bản copy denormalized; để client tự khai nghĩa là sản phẩm gắn nhãn danh mục mà nó không thuộc về. Server tự lấy từ `categoryId`, và **`CategoryService.update` giờ cập nhật lại `categoryName` cho mọi sản phẩm khi đổi tên danh mục** — đúng khoản nợ ghi ngày 19/8 ("xử lý khi port module products").
63. **Xoá variant giờ kiểm đủ 4 bảng tham chiếu** (inventory, order item, stock movement item, promotion item), bản cũ chỉ kiểm inventory. Trong Mongo mấy tham chiếu kia chỉ là ref treo; ở Postgres là FK thật, nên check cũ sẽ nổ thành lỗi ràng buộc không nói được gì cho người dùng.
64. **Xoá module `product-items`** (CRUD sinh tự động ở `/product-items`). Để lại là có một đường vòng tạo/xoá variant **không đi qua** kiểm SKU trùng, không đi qua các check ở mục 63. Variant giờ chỉ vào được qua `/products/...`. `Product`/`ProductItem`/`Inventory` đã thêm vào `PORTED_MODELS`.
65. **Quyền inventory**: thêm/gỡ mặt hàng ở một địa điểm bản cũ chặn bằng `role in (TENANT_OWNER, WAREHOUSE_MANAGER)`. Cả role đó lẫn cách chặn theo role đều không còn — giờ là `inventory:create` / `inventory:delete`, tenant tự quyết cấp cho role nào (đúng cách đã thay thế cho việc bổ nhiệm quản lý chi nhánh/kho).

**Dùng chung mới**

66. **`src/common/dto/location-ref.dto.ts`** — Inventory trỏ tới "chi nhánh hoặc kho": Mongo lưu `locationId`+`locationType`, Postgres lưu cặp FK nullable `branch_id`/`warehouse_id`. File này là **chỗ duy nhất** map giữa hai bên, API giữ nguyên cặp cũ nên frontend không phải sửa (cùng kiểu đánh đổi với `attendance-location.dto.ts`). `toLocationColumns` cho write, `toLocationRef` cho response, `locationWhere` cho filter. `LocationRefQueryDto` giữ luật "có locationId thì bắt buộc có locationType", các query DTO ghép bằng `IntersectionType` thay vì chép lại. Có unit test.
67. **`src/common/constants/product-status.ts`** và **`location-type.ts`** — bản cũ chép mảng `["ACTIVE","INACTIVE","DISCONTINUED"]` ở 3 DTO khác nhau. `location-type.ts` cố ý viết thường (`"branch"/"warehouse"`) vì đó đúng là chuỗi frontend đang gửi và đọc.
68. **`NotificationService.managersOfLocation()`** đã port — nhưng đọc `Branch.managerId`/`Warehouse.managerId` thay cho role BRANCH_MANAGER/WAREHOUSE_MANAGER đã bị xoá, và **fallback về chủ cửa hàng** khi địa điểm chưa có quản lý. Bản cũ trả mảng rỗng, tức là cảnh báo tồn kho thấp của một chi nhánh chưa có quản lý gửi cho **không ai cả**.

**Primitive tồn kho (để dành cho Order / StockMovement)**

69. `adjustStock` / `lowStockCrossing` / `notifyLowStock` nằm trong `InventoryService` chứ không nằm ở module gọi nó — "kho thay đổi thế nào và khi nào thì cảnh báo" là **một luật**, chép một bản sang bán hàng và một bản sang chuyển kho là cách chắc chắn để hai bên lệch nhau. Hai điểm phải nhớ khi port Order/StockMovement:
    - `adjustStock` và `initializeStock` nhận **transaction client**, không phải `this.prisma`. Đơn hàng rollback mà kho vẫn bị trừ là failure mode ở đây.
    - `notifyLowStock` gọi **sau khi transaction commit** và không bao giờ ném lỗi. Kho đã trừ, đơn đã xong rồi — lỗi gửi thông báo không được phép làm hỏng ca bán hàng.
70. Luật cảnh báo tách thành hàm thuần `crossedLowStock` (`low-stock.ts`) có unit test riêng, giống cách `nextSubscriptionStatus` đã làm. Nó **edge-trigger**: chỉ bắn đúng bước vượt ngưỡng. Bắn mỗi lần `stock <= minStock` nghĩa là mỗi lần bán tiếp một món đang thiếu lại bắn thêm một cái, và quản lý tắt noti ngay ngày đầu — lúc đó cảnh báo thật cũng không ai thấy nữa. Test viết theo đúng failure mode đó.

**Đổi API mà frontend sẽ thấy** (cần báo team FE)

71. `DELETE /products/:id/delete` → **`DELETE /products/:id`**, và `DELETE /products/items/:itemId/delete` → **`DELETE /products/items/:itemId`**. Hậu tố `/delete` không chỗ nào khác trong cả hai codebase dùng. Nếu FE chưa sửa kịp thì nói, thêm lại alias không tốn gì.
72. `VAT` → `vat` trong request/response của variant, khớp tên cột.
73. `GET /inventory` trả `location: {locationId, locationType}` (object) thay vì hai field phẳng `locationId`/`locationType` ở gốc như bản cũ.

**Kiểm chứng**: `tsc --noEmit` sạch · `pnpm run lint` sạch · `pnpm run build` sạch · `node scripts/check-permissions.js` OK (không phải sửa seed — `products:*` và `inventory:*` đã có đủ trong catalog) · unit **22/22** (thêm `low-stock.spec.ts` và `location-ref.spec.ts`) · `test/di-check.e2e-spec.ts` 2/2, đã thêm assert cho `ProductService`/`InventoryService`.

**CHƯA chạy được với database thật** — Docker Desktop vẫn không chạy trên máy (lần thứ hai, sau đợt 20/8). Nghĩa là **chưa có dòng nào của module này chạm vào Postgres thật**: chưa verify được quota, transaction tạo sản phẩm + variant + tồn kho ban đầu, cross-tenant 404, hay filter `isLowStock` (dùng field-reference của Prisma `stock <= minStock` — đây là chỗ tôi ít chắc nhất, nó compile được nhưng chưa từng chạy). Việc đầu tiên của phiên sau: `docker compose up -d && pnpm run test:e2e`, rồi test tay theo kịch bản như đợt 19/8.

### 2026-08-25, phần 2 — port thật Staff (5 route còn thiếu) + Stock Movement

**Staff — 5 route còn thiếu, gắn vào module `users`**

74. `POST|PATCH /users/:id/leave-balance`, `POST /users/:id/account`, `PATCH /users/:id/account/password`, `PATCH /users/:id/account/deactivate`. Giữ nguyên sub-path cũ, chỉ đổi tiền tố `/staff` → `/users` (đã đổi từ 17/8). Quyền dùng **`users:update`** chứ không phải `staff:update`: catalog có cả hai resource cho cùng một thứ, mà module này xưa nay check `users` — để hai resource song song chính là cách một quyền bị cấp ở chỗ này nhưng kiểm ở chỗ kia. `staff:*` giờ là legacy.
75. **`GET /staff/roles` không port lại.** Nó trả enum role cố định mà người gọi được phép gán; role giờ là dòng dữ liệu do tenant tự định nghĩa nên câu trả lời đúng là `GET /roles`.
76. **Phần lớn `StaffService` cũ (1129 dòng) là plumbing phân cấp role** — ai được sửa ai, theo BRANCH_MANAGER / WAREHOUSE_MANAGER / STAFF. Không port dòng nào: các role đó đã bị xoá từ đợt RBAC, "ai được sửa nhân viên" giờ là đúng một quyền.
77. **Xoá nhân viên giờ ẩn danh dữ liệu** (`anonymizeDeletedStaff` bản cũ): giữ dòng (orders/attendances/payslips/audit log đều trỏ FK vào) nhưng xoá dữ liệu cá nhân, và `phoneNumber` thành `deleted_<id>` — nó là handle đăng nhập, để nguyên là **không bao giờ tuyển lại được người đó bằng số cũ**. Lần port NestJS trước rút gọn hàm này còn mỗi việc đổi status; giờ đã đủ, kèm `deletedById` + `deletionReason` (2 cột có sẵn mà chưa ai ghi).
78. **Vô hiệu hoá tài khoản xoá luôn password** ngoài việc đổi status, nên token đang cầm bị chặn ngay request kế tiếp (INACTIVE nằm trong `INACTIVE_USER_STATUSES`).
79. **Hai guard chung cho cả deactivate và delete**: (a) không được đang là người nhận bàn giao của đơn nghỉ còn hiệu lực; (b) không được đang là quản lý của chi nhánh/kho. Bản cũ nhận `replacementManagerId` rồi tự swap quản lý ngay trong hàm; giờ việc bổ nhiệm là `PATCH /branches/:id/manager`, nên ở đây **từ chối và chỉ sang đó** — một luật bổ nhiệm, một chỗ.
80. **Leave balance**: `remainingDays` được **tính lại** = hạn mức mới − số ngày đã dùng, không ghi đè, nên nâng quota giữa năm không trả lại ngày đã nghỉ. `POST` (khởi tạo) chỉ hợp lệ khi chưa dùng ngày nào. Cả hai đường ghi đều qua `updateMany` có điều kiện giá trị cũ — hai quản lý sửa cùng lúc, hoặc sửa lúc đơn nghỉ vừa duyệt, sẽ 409 thay vì đè lên nhau (đúng trick `SupplierService.payDebt`). Thêm chặn trên `Max(365)` — DTO cũ nhận mọi số nguyên không âm nên gõ nhầm là ai đó có 3650 ngày phép.

**Stock Movement — 10 route, đủ máy trạng thái**

81. `POST`, `GET`, `GET /:id`, `PATCH /:id/{details,open,close,ship,receive,approve-adjust,cancel}` tại `/stock-movements`, đúng path và đúng quyền bản cũ (`approve` cho ship + duyệt kiểm kê, `receive`, `cancel`). **Không có DELETE** — phiếu là chứng từ về hàng đã đi thật, chỉ huỷ chứ không xoá. Cặp `stock_movement:delete` mà CRUD sinh tự động đẻ ra giờ thành không dùng.
82. **4 loại phiếu, 2 dạng vòng đời**: `EXPORT`/`RETURN` (giữa 2 địa điểm của mình) đi `DRAFT → OPENING → CLOSED → IN_TRANSIT → RECEIVED`; `IMPORT` (từ NCC) và `ADJUST` (kiểm kê tại 1 địa điểm) mở thẳng ở `PENDING` vì không có gì để soạn hàng, và `ADJUST` kết thúc ở `COMPLETED` chứ không phải `RECEIVED` vì chẳng có gì tới nơi.
83. **Kho chỉ đổi ở 4 chỗ**: `ship` trừ nguồn (chỉ phiếu chuyển), `receive` cộng đích theo số **thực nhận** (giao thiếu là bình thường, phần thiếu đơn giản là không cộng), `approve-adjust` áp `thực đếm − hệ thống`, `cancel` trả lại phần `ship` đã trừ nếu phiếu còn IN_TRANSIT. Cả 4 đều ghi kho và ghi status **trong cùng một transaction**, và bắn thông báo **sau khi commit**.
84. **Hạn mức công nợ NCC đã được kiểm — đúng khoản nợ ghi từ 19/8.** Kiểm ở 3 chỗ: lúc tạo phiếu, lúc sửa details, và **lại lần nữa bên trong transaction nhận hàng** sau khi cộng nợ (nhiều phiếu nhập có thể mở cùng lúc với một NCC, chỉ phiếu thật sự về mới tính; vượt ở đây thì ném lỗi và rollback cả phiếu nhận). `creditLimit <= 0` nghĩa là không giới hạn, khớp cách seed.
85. **Cảnh báo 75% hạn mức** tách thành hàm thuần `crossedCreditWarning` có unit test riêng, **edge-trigger** y hệt `crossedLowStock`: chỉ bắn đúng phiếu vượt ngưỡng, không bắn mỗi phiếu nhập sau đó. Đây là lần thứ hai áp cùng một khuôn — nếu sau này có cảnh báo ngưỡng thứ ba thì làm y như vậy.
86. **Phân quyền theo địa điểm là THAY THẾ, không phải port.** Bản cũ rẽ nhánh theo BRANCH_MANAGER / WAREHOUSE_MANAGER và theo `managedScheduleAccess` (quyền tạm do lịch làm việc cấp). Không cái nào còn: 2 role đã xoá, còn module WorkingSchedule nằm trong nhóm hoãn. Thay bằng: TENANT_OWNER thao tác được mọi địa điểm của tenant, STAFF thao tác được đúng nơi mình trực thuộc (`User.branchId`/`warehouseId`). **`canActAt()` là seam duy nhất** — khi port WorkingSchedule thì nới đúng chỗ đó, không đụng gì khác.
87. Hai luật theo role bị **bỏ hẳn thay vì dịch**: "branch manager không được tạo IMPORT" (giờ là chuyện ai được cấp `stock_movement:create`) và "branch manager không được EXPORT về kho" — cái sau sống sót dưới dạng luật về chính cái phiếu: chi nhánh → kho là RETURN, ai làm cũng vậy (`assertTransferMakesSense`).
88. `OPEN_MOVEMENT_STATUSES` (danh sách trạng thái "phiếu chưa xong") giờ khai một lần ở `stock-movement.constants.ts`; `ProductService` (chặn ngừng kinh doanh sản phẩm đang nằm trong phiếu dở) import từ đó thay vì giữ bản chép riêng — bản chép đó tôi vừa tạo hôm nay ở mục 63, gom lại luôn trước khi nó kịp lệch.
89. `StockMovementRequest` đã thêm vào `PORTED_MODELS`. `NotificationService.managersOfLocation` (port hôm nay ở mục 68) được dùng thật ở đây — thông báo đi tới quản lý địa điểm gửi/nhận, và **người bấm nút luôn bị loại khỏi danh sách nhận**: không ai cần được báo về việc mình vừa làm.

**Kiểm chứng**: `tsc --noEmit` sạch · `pnpm run lint` sạch · `pnpm run build` sạch · `node scripts/check-permissions.js` OK (không phải sửa seed) · unit **28/28** (thêm `credit-warning.spec.ts`) · di-check 2/2, đã thêm assert cho `StockMovementService` và `UserService`.

**Vẫn CHƯA chạm database thật** — Docker Desktop vẫn không chạy (lần thứ ba). Với stock movement thì đây là thiếu sót nặng hơn hẳn product/inventory: **toàn bộ máy trạng thái, mọi transaction cộng/trừ kho, và cả 3 điểm kiểm hạn mức công nợ đều chưa từng chạy một lần nào.** Đừng commit trước khi test tay đủ 4 loại phiếu qua từng bước.

### 2026-08-25, phần 3 — review đối chiếu bản cũ rồi sửa những gì lệch

Đọc lại 4 module vừa port và so từng luồng với `iKiotMS-BE`. Review nêu 3 lỗi thật, 8 đổi hành vi chưa báo, 2 chỗ nới quyền, 3 chỗ thiếu. Chủ dự án chọn sửa: 1.1, 1.3, revert 2.5 + 2.6, và bù 5.2 + 5.3.

90. **`PATCH /users/:id` từng đi vòng qua toàn bộ guard vô hiệu hoá tài khoản.** `UpdateUserDto` nhận `status` và `update()` ghi thẳng, nên set `INACTIVE` qua đây **bỏ qua** cả 3 thứ mới thêm hôm nay: không kiểm người đang nhận bàn giao, không kiểm đang là quản lý chi nhánh/kho, **không xoá password** (bật lại là đăng nhập được bằng mật khẩu cũ). Bản cũ `updateStaff` **cấm hẳn** field này — `if (Object.hasOwn(data, "status")) throw` — chính là để bắt đi qua route deactivate. Đã bỏ `status` khỏi DTO. Vòng đời tài khoản giờ chỉ đi qua `POST /users/:id/account` và `PATCH /users/:id/account/deactivate`.
91. **`adjustStock` trả lại tính nguyên tử.** Bản cũ là `findOneAndUpdate({$inc}, {upsert:true})` — một lệnh. Bản tôi viết là `findFirst` → `create`/`update`, có khe hở: hai lần nhận hàng đồng thời vào một địa điểm **chưa từng có** mặt hàng đó, cả hai đọc ra null, cả hai insert, một cái chết vì unique index. Giờ dùng `upsert`. **Phải chọn đúng 1 trong 2 unique index tuỳ đầu nào được set** — trong Postgres unique index có cột NULL thì không ràng buộc gì, nên `(tenant, branch, item)` chỉ có tác dụng với dòng chi nhánh và `(tenant, warehouse, item)` chỉ với dòng kho.
92. **Revert 2 response về đúng bản cũ**: `DELETE /inventory/:id` trả `{success: true}` (không phải `{id, removed}`), và 2 route leave-balance trả `{message, data, leaveBalance}` (không phải `{user, leaveBalance}`).
93. **`GET /users` giờ có phân trang + filter** như `getStaffList` cũ: `page`/`limit`/`search`/`status`/`roleId`/`branchId`/`warehouseId`, trả envelope `{data, pagination}`. Trước đó trả **toàn bộ nhân viên, không phân trang, không filter** — tenant vài trăm người là payload không dùng được. Giữ 2 luật của bản cũ: **chỉ hiện tài khoản STAFF** (chủ cửa hàng không nằm trong danh sách nhân viên của chính mình) và **loại chính người gọi** ra khỏi danh sách. Đổi tên tham số `recordPerPage`→`limit`, `keyword`→`search` cho khớp 5 list endpoint đã port khác; `role` (enum cũ) → `roleId` (uuid, vì role giờ là dữ liệu).
94. **`PATCH /users/:id` giờ sửa được cả hồ sơ** — email, `hireDate`, `paysheetId`, `accountNote` và object `profile` lồng (firstName, lastName, avatarUrl, dob, taxNumber, identificationId, address, gender). Trước đó chỉ có 4 field, hồ sơ nhân viên **không có đường nào vào cả**. `paysheetId` phải là bảng lương ACTIVE của tenant (port `validatePaySheetAssignment`).
95. **Port `normalizeWorkplaceUpdateData`**: gửi `branchId` thì tự null `warehouseId` và ngược lại; gửi cả hai là 400. Thiếu nó thì một PATCH chỉ gửi một nửa sẽ để lại nơi làm việc cũ → dòng dữ liệu khai 2 nơi làm việc, và lần sửa **sau** mới báo lỗi, đổ tội cho người sửa sau.
96. **Port validator CCCD** thành hàm thuần `validateVietnamIdentificationId` có unit test (`src/modules/users/vietnam-identification.ts`): 12 chữ số, mã tỉnh nằm trong danh sách 63 mã, và **thế kỷ/năm sinh + giới tính mã hoá trong số CCCD phải khớp `dob`/`gender` của hồ sơ**. Cả ba field đều có thể là field đang được sửa nên kiểm trên kết quả đã merge, đúng như bản cũ. Đây là rule đáng giá: CCCD, ngày sinh và giới tính đều chảy xuống bảng lương và export bảo hiểm xã hội, sai ở đó là chuyện của người khác.
97. **Kiểm trùng email + CCCD** khi sửa (port `checkStaffUniqueness`). **Một điểm cố ý khác bản cũ**: bản cũ kiểm CCCD trùng **toàn hệ thống, không lọc tenant** — hợp lý cho một số định danh quốc gia, nhưng nghĩa là cửa hàng A dò được cửa hàng B có tuyển một người cụ thể hay không. Đã thu về phạm vi tenant.
98. `SELECT_SAFE` mở rộng thêm các cột vừa cho sửa (profile\*, hireDate, paysheetId, accountNote) — nếu không thì PATCH xong response không hiện thứ vừa ghi.

**Chưa sửa (review có nêu, chủ dự án chưa chọn)**: 1.2 (`findOne` không loại DELETED — nhưng bỏ `status` khỏi DTO ở mục 90 đã chặn đường hồi sinh tài khoản đã xoá, còn lại là sửa được roleId/posting của một dòng đã xoá), 2.1–2.4 và 2.7–2.8 (đổi default limit, thêm sort, filter `locationType` đơn lẻ, đổi shape response stock-movement), 3.x (các chỗ siết chặt hơn bản cũ), 4.x (bên nhận huỷ được phiếu; `approveAdjust` bắn cảnh báo tồn kho), 5.1 (revoke refresh token khi xoá/vô hiệu hoá — chờ Redis).

**Còn thiếu ở nhánh tạo tài khoản**: validator số điện thoại VN của bản cũ (`validateStaffPhoneNumber` — chặn đầu số VoIP/vệ tinh/mạng dùng riêng, tổng đài khẩn cấp) chưa port; `CreateUserDto` vẫn chỉ có `@MinLength(8)`, tức là nhận `"abcdefgh"`. Nằm ở nhánh create nên để ngoài phạm vi lần sửa này.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **42/42** (thêm `vietnam-identification.spec.ts`) · di-check 2/2. **Vẫn chưa chạm database thật** — Docker vẫn không lên.

### 2026-08-25, phần 4 — validator số điện thoại + **chạy thật trên Postgres lần đầu**

99. **Port `validateStaffPhoneNumber`** thành hàm thuần `validateVietnamPhoneNumber` có unit test (21 case): đầu số nhà mạng hợp lệ, và chặn riêng từng dải trông giống di động nhưng không thể là — VoIP 065, vệ tinh 067, khối 069 của cơ quan nhà nước, 080, và tổng đài 111–115, **mỗi dải một message riêng** (báo "số điện thoại không hợp lệ" cho một số 113 đang hoạt động thì người nhập không hiểu sai ở đâu). Dải khẩn cấp kiểm **trước** luật 10 chữ số, nếu không thì message "phải đủ 10 chữ số" bắn trước và giải thích nhầm. Bỏ `@MinLength(8)` khỏi `CreateUserDto.phoneNumber` — nó nhận `"abcdefgh"`.

**Docker đã lên. Đây là lần đầu code của 3 đợt port hôm nay chạm database thật.**

100. **Thêm `test/smoke.e2e-spec.ts`** — 13 test, 46 assertion, chạy trên Postgres thật: đăng ký tenant → trial → chi nhánh/kho/NCC → sản phẩm 2 biến thể + tồn kho đầu kỳ → toàn bộ máy trạng thái chuyển kho. **Tự dọn sạch dữ liệu nó tạo ra** (đã verify DB không còn dòng thừa), nên chạy lại bao nhiêu lần cũng được — giữ đúng tính chất đó nếu viết thêm.
101. **Hai chỗ tôi lo nhất đều chạy đúng**: (a) `isLowStock` dùng Prisma field reference (`stock <= minStock`) — lọc đúng 1 dòng tồn 8/ngưỡng 10; (b) `adjustStock` bản `upsert` **tạo được dòng tồn kho chưa tồn tại** khi nhận hàng nhập (kho 0 → 60).
102. **Số học tồn kho đúng qua cả vòng đời**: xuất 20 (kho 50→30) → nhận thiếu 18/20 (chi nhánh 8→26) → huỷ phiếu đang IN_TRANSIT trả lại kho (25→30) → kiểm kê đếm 24 vs hệ thống 26 → duyệt (chi nhánh 26→24). Chặn xuất quá tồn (400), chặn ship từ DRAFT (409).
103. **Hạn mức công nợ đúng cả 3 điểm kiểm**: tạo phiếu 15tr > hạn mức 10tr → 400; nhận 60 × 130k → công nợ đúng 7.800.000; giá nhập > giá bán lẻ → 400.
104. **Cảnh báo 75% hạn mức**: lúc đầu test fail và **hoá ra tôi test sai, không phải code sai** — chủ cửa hàng tự nhận hàng thì người nhận thông báo duy nhất lại chính là người vừa bấm, bị lọc ra, nên không gửi gì. **Bản cũ hành xử y hệt** (`ownerIdsFiltered.length > 0`). Đã sửa test: cho nhân viên nhận hàng → chủ cửa hàng nhận đúng 1 thông báo; và phiếu nhận **thứ hai** trên ngưỡng vẫn chỉ 1 thông báo, đúng tính edge-trigger.
105. **Phân quyền theo địa điểm chạy đúng**: nhân viên chi nhánh chỉ thấy phiếu của chi nhánh mình, tạo phiếu xuất từ kho khác → 403, gọi `/products` khi role không có `products:read` → 403.
106. **Transaction tạo sản phẩm rollback sạch**: tạo sản phẩm có SKU trùng → 409 và **không để lại nửa sản phẩm** nào trong DB.
107. Thêm override ESLint cho `test/**/*.e2e-spec.ts`: tắt nhóm `no-unsafe-*`. Body của response supertest là `any` theo bản chất và assert lên nó chính là việc của e2e test — ép kiểu từng dòng chỉ làm assertion khó đọc chứ không an toàn hơn. Code trong `src/` giữ nguyên đủ luật.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **63/63** · **e2e 17/17 trên Postgres thật** · DB không còn dữ liệu test.

**Quan sát nhỏ, chưa xử lý**: chạy e2e có warning `pg` — *"Calling client.query() when the client is already executing a query is deprecated"*. Không làm fail test, nhiều khả năng đến từ các cặp `Promise.all([findMany, count])` dùng chung một connection qua driver adapter. Đáng xem lại khi nâng `pg` lên 9.

### 2026-08-26 — port thật Order + Promotion (+ Customer)

Nhóm 5. Ba module đi cùng nhau: giỏ hàng được `promotions` tính giá, `orders` chốt đơn, gắn vào một dòng `customers`.

**Route** (12 + 9 + 6)

108. `orders`: `POST`, `GET`, `GET /:id`, `PATCH /:id/status`, `POST /:id/pay-offline`, và `POST /webhook/sepay/order` (public). **Không có DELETE** — đơn sai thì CANCELLED hoặc RETURNED, cả hai đều để lại dấu vết.
109. `promotions`: CRUD + `GET /:id/logs` + 3 endpoint giỏ hàng `POST /promotions/{candidates,calculate,apply}`. Chỉ `/apply` ghi dữ liệu.
110. `customers`: CRUD + `DELETE /customers` hàng loạt. Xoá mềm (`isDeleted`) vì đơn hàng trỏ FK vào.
111. **Xoá module `promotion-logs` sinh tự động** — cùng lý do đã xoá `product-items`: nó mở `/promotion-logs` CRUD, tức là một đường ghi log sử dụng khuyến mãi **không đi qua** `/promotions/apply` và các hạn mức ở đó.

**Lỗ hổng đã vá**

112. **Tổng tiền đơn hàng giờ do server tính, không nhận từ client.** Bản cũ lấy `grandTotal` thẳng từ body rồi lưu — một request tự chế là quét cả giỏ hàng với giá 0. Đây chính là khoản nợ CLAUDE.md ghi từ lúc dựng CRUD sinh tự động, giờ trả xong. `grandTotalOf` = tổng dòng − giảm giá từng dòng − giảm giá `ORDER` thủ công. Giảm giá kiểu `PROMOTION` **không trừ lần nữa**: engine đã rải nó vào từng dòng rồi, `discountValue` chỉ là con số ghi lại. `status`/`change`/`paymentReference` cũng do server quyết.
113. **6 route customer của bản cũ chạy `verifyJwt` trần, không `authorize()`** — ai đăng nhập cũng đọc và xoá được toàn bộ danh sách khách hàng. Resource `customers` đã thêm vào catalog từ đợt RBAC đúng để vá chỗ này, giờ mới gắn thật.
114. `customerPay < grandTotal` giờ bị chặn ở cả tạo đơn lẫn `pay-offline` — chênh lệch giữa "tiền thối" và một khoản hụt âm thầm vào doanh thu.

**Pricing engine**

115. **`pricing-engine.ts` không chạm database** — port đúng cấu trúc bản cũ (`PricingEngine.js` vốn đã thuần), thành hàm thuần có **26 unit test**. Mọi luật về tiền nằm ở đó; service lo phần tra cứu: khuyến mãi ứng viên, category của từng dòng giỏ (biến thể không mang category, sản phẩm cha mới mang), và số lần khách đã dùng.
116. **Áp khuyến mãi luôn là tường minh** — caller truyền đúng id người dùng bấm; engine không bao giờ tự đoán tổ hợp "tốt nhất", và một khuyến mãi được chọn nhưng không đủ điều kiện là **400 chứ không im lặng bỏ qua** (bỏ qua nghĩa là thu của khách nhiều hơn số hiện trên màn hình).
117. **Tối đa 2 khuyến mãi, và phải cùng stackable**; discount **tích luỹ** trên mỗi dòng bị kẹp lại bằng giá dòng đó. Thiếu cái kẹp này thì 2 khuyến mãi cùng trúng một SKU sẽ giảm quá giá và đơn hàng thành nợ tiền khách. Có test riêng cho đúng tình huống đó.
118. **Hạn mức lượt dùng kiểm lại BÊN TRONG transaction** ở `/apply`: hạn mức tổng bằng `updateMany` có điều kiện (chỉ tăng khi còn chỗ), hạn mức theo khách bằng đếm lại log, và `@@unique([orderId, promotionId])` làm chốt chặn cuối. Số đọc trước đó có thể đã cũ — hai quầy, hoặc một khách mở hai tab.

**Dòng tiền & SePay**

119. **Bán tiền mặt có thối là 2 dòng cash flow**, không phải 1: ngăn kéo thật sự nhận cả tờ tiền và thật sự thối lại, kiểm quỹ cuối ca phải khớp cả hai. **Chỉ dòng INCOME mang `orderId`** — `@@unique([orderId, flowType])` phải để trống chỗ cho dòng EXPENSE mà lần RETURN sau này ghi. Bản cũ cũng để dòng thối không gắn orderId; nhìn như sơ suất nhưng không phải.
120. **`SepayOrderService` tách hẳn khỏi `SepaySubscriptionService`** đúng như CLAUDE.md cảnh báo từ đợt subscription: bên kia trả vào tài khoản của iKiot lấy từ env, bên này trả vào tài khoản **của từng tenant** lấy từ cột `banking.*`, và **khoá webhook chính là cách nhận diện tenant**. Gộp lại nghĩa là credential của tích hợp này thanh toán được hoá đơn của tích hợp kia.
121. Webhook trả 200 cho mọi trường hợp để SePay không retry, và **log cảnh báo** khi tiền về cho đơn đã thanh toán bằng cách khác — chỗ đó cần hoàn tiền thủ công.

**Hai thay thế do RBAC**

122. Phạm vi xem khuyến mãi: bản cũ rẽ theo BRANCH_MANAGER/STAFF; giờ là "TENANT_OWNER thấy hết, STAFF thấy khuyến mãi toàn hệ thống + khuyến mãi của chi nhánh mình". Luật "branch manager chỉ được tạo khuyến mãi cho chi nhánh mình" bỏ theo role — tạo được hay không là chuyện ai giữ `promotions:create`.
123. Thông báo đơn đã thanh toán: bản cũ emit vào room `order:<id>` mà client tự join. Cơ chế đó **đã bị gỡ như một bản vá bảo mật** (xem CLAUDE.md "Realtime"), nên giờ emit vào `tenant:<id>` kèm orderId trong payload để client tự lọc.

**Kiểm chứng — chạy thật trên Postgres**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **89/89** (thêm 26 test pricing engine) · **e2e 26/26**, smoke suite mở rộng thêm 9 kịch bản: tính tổng server-side, chặn bán quá tồn, RETURN trả lại kho + ghi dòng hoàn tiền, SEPAY mở PENDING → `pay-offline` → chặn thu hai lần, webhook (khoá sai / trích ref từ text tự do / chặn replay), tính giá có cap, `/apply` tăng `usedCount` + ghi log, chặn dùng quá hạn mức, phạm vi chi nhánh, khách vãng lai tự tạo, xoá mềm khách hàng. DB sạch sau khi chạy.

### 2026-08-26, phần 2 — review đối chiếu bản cũ rồi sửa

Review nêu 3 lỗi thật, 6 chỗ siết chặt chưa ghi, 7 đổi response, 3 yếu điểm kế thừa. Chủ dự án chốt: **sửa lỗi, giữ nguyên phần đổi response (không ảnh hưởng logic/nghiệp vụ), và làm cho đúng phần yếu điểm.**

**Lỗi**

124. **`@Permissions` giờ nhận nhiều action với nghĩa "hoặc"** — `@Permissions('orders', 'update', 'pay_offline')`. Bản cũ là `authorize("orders", ["update","pay_offline"])` và middleware giải bằng `actions.some(...)`, tức là **có một trong hai là đủ**; tôi port thành mỗi `pay_offline` nên một role đang có `orders:update` sẽ mất quyền sau khi lên bản mới. Đã sửa cả decorator, guard, và regex trong `scripts/check-permissions.js` (mọi action liệt kê vẫn phải có trong catalog).
125. **Tách `can(user, resource, action)`** ra `common/utils/permission.ts` — đúng luật guard đang dùng, để service hỏi được. Cần vì có quyết định guard không làm được: vào được `GET /orders` là `orders:read`, nhưng **thấy bao nhiêu** mới là `orders:view_all`. Guard giờ gọi chung hàm này, không tự lặp lại phần short-circuit ADMIN/TENANT_OWNER nữa.
126. **`appliedPromotions[].promotionId` giờ kiểm tenant.** `OrderAppliedPromotion.promotionId` là FK thật, nên id khuyến mãi của tenant khác vẫn qua được FK và **liên kết dữ liệu hai tenant với nhau**. Ở Mongo nó chỉ là ref treo nên không ai để ý.
127. Bỏ mảng `crossings` chết trong `updateStatus` — khai rồi không bao giờ push, gọi `notifyLowStock([])`. Thay bằng comment nói rõ **vì sao** không cần: huỷ/hoàn chỉ làm kho tăng, mà tăng thì không thể tụt qua ngưỡng.

**Yếu điểm kế thừa — làm cho đúng**

128. **`InventoryService.deductStock`** — trừ kho có chốt `stock: { gte: quantity }` **ngay trong lệnh UPDATE**. Trước đó (và ở bản cũ) là đọc mức tồn rồi trừ sau: hai quầy cùng thấy còn 1 món, cùng bán, kho xuống âm. Giờ `updateMany` hoặc khớp và trừ nguyên tử, hoặc không khớp — và "không khớp" chính là câu trả lời cho "có đủ hàng không". **Áp cho cả bán hàng lẫn `ship` phiếu chuyển kho** — cùng một luật, một chỗ. Các lệnh `assertSourceStock`/`assertStockCovers` còn lại giờ chỉ là báo sớm cho người dùng, không phải chỗ chặn.
129. **Khách vãng lai: `upsert` trong transaction.** Hai vấn đề cùng lúc — (a) tạo trước khi validate xong nên đơn lỗi vẫn để lại dòng khách, (b) find-then-create nên hai đơn vô danh đồng thời tạo **hai** dòng khách vãng lai. Giờ giải trong transaction bằng `upsert`, dựa trên index mới.
130. **Migration `20260826050000_customer_code_unique_per_tenant`** — `@@unique([tenantId, customerCode])`. Viết tay vì `prisma migrate dev` cần terminal tương tác (môi trường này không có), rồi `prisma migrate deploy`. Cột nullable nên Postgres chỉ ràng buộc dòng **thực sự có mã** — đúng luật cần: bao nhiêu khách không mã cũng được, không bao giờ hai khách chung một mã. Đây cũng là index cho phép `upsert` ở mục 129, và nó biến việc kiểm trùng `customerCode` ở tầng app thành có bảo chứng dưới DB.
131. **`GET /orders` và `GET /orders/:id` giờ giới hạn theo chi nhánh** — nhân viên thấy đơn của chi nhánh mình, `orders:view_all` mở ra toàn tenant (chủ/admin luôn qua). Bản cũ **không giới hạn gì**: thu ngân chi nhánh A đọc được toàn bộ doanh thu chi nhánh B. `orders:view_all` nằm trong catalog từ đầu mà không ai dùng — đọc đúng như luật định làm mà chưa nối. Giờ khớp với `stock-movements`, hai màn hình cạnh nhau mà hành xử khác nhau thì tự nó là một bug report.

**Giữ nguyên theo yêu cầu**: phần đổi response (mất field `name` của nhân viên, `GET /customers` giới hạn 10 đơn/khách, limit mặc định 20→10, `qrUrl: null` thay vì bỏ field, status 400→409, `productName` lấy từ DB) — đều không đổi logic hay nghiệp vụ. 6 chỗ siết chặt hơn bản cũ cũng giữ, giờ đã ghi vào CLAUDE.md.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **89/89** · **e2e 30/30 trên Postgres thật**. Smoke thêm 4 kịch bản đúng vào các fix: đơn mang khuyến mãi của tenant khác → 404; nhân viên `GET /orders` chỉ thấy chi nhánh mình còn chủ thấy hết; bán vượt tồn 1 đơn vị → 400 **và tồn kho không suy suyển** (chốt nằm trong UPDATE); **3 đơn vô danh chạy song song → đúng 1 dòng khách vãng lai**.

### 2026-08-26, phần 3 — port thật Cash Drawer (nốt nhóm 5)

**Một lỗi schema phải sửa trước khi port**

132. **Index `(tenant_id, branch_id, status)` là unique ĐẦY ĐỦ, không phải partial.** Schema đã ghi chú "partial: WHERE status = 'OPEN' — add via follow-up migration edit" nhưng migration init tạo ra bản đầy đủ. Nghĩa là mỗi chi nhánh chỉ **đóng ca được đúng một lần trong đời** — ngày thứ hai gọi `finalize` sẽ nổ unique violation. Đây là loại lỗi chỉ lộ ra ở ngày thứ hai chạy thật.
    Migration `20260826060000_cash_drawer_single_open_session`: DROP index cũ, CREATE unique **partial** `WHERE status = 'OPEN'`. Đã gỡ `@@unique` khỏi `schema.prisma` vì Prisma không diễn đạt được WHERE — kèm cảnh báo trong schema: lần `prisma migrate dev` sau sẽ đề nghị xoá index này, **phải sửa migration sinh ra chứ đừng cho chạy thẳng**.

**Module** (6 route, đúng path và đúng quyền bản cũ)

133. `POST /cash-drawer-sessions` (open), `GET /current`, `GET`, `GET /:id`, `POST /:id/shift-logs`, `POST /:id/finalize`. **Không có PATCH/DELETE** — ca quầy là chứng từ tiền bạc, chỉ mở/ghi/chốt chứ không sửa. Ba route đọc dùng `@Permissions('cash_drawers', 'read', 'read_own')` — đúng dạng "hoặc" của `authorize(["read","read_own"])` bản cũ, nhờ mục 124 hôm nay.
134. **`businessDate()` tách thành hàm thuần có 6 unit test.** Ngày kinh doanh là ngày **theo giờ cửa hàng** (`Asia/Ho_Chi_Minh`), không phải UTC — trả về nửa đêm UTC của ngày đó để cột `date` của Postgres không trôi. Tính theo UTC thì mọi ca mở trước 07:00 giờ VN bị tính sang doanh thu hôm trước; sai kiểu này không ai thấy cho tới lúc kiểm quỹ lệch hẳn một ca.
135. **Trình tự phiếu ca được kiểm chứ không tin.** `START` chỉ hợp lệ khi là phiếu đầu tiên, hoặc ngay sau một `END` có ghi tên chính người này nhận bàn giao. `END` chỉ hợp lệ từ đúng người đã ghi `START` tương ứng. Và **chỉ người đang giữ quầy mới ghi được**. Không có mấy luật đó thì hai thu ngân cùng khai đã giữ quầy trong một khoảng, và khi thiếu tiền thì không quy được cho ca nào.
136. `END` có `nextStaffId` = bàn giao: quầy sang tay người đó, ca vẫn mở. `END` không ghi tên ai = ca cuối ngày, và đó là điều kiện để `finalize`. Mọi lệnh ghi đều chốt trên `updatedAt` vừa đọc — một phiếu chen ngang sẽ làm phần kiểm trình tự phía trên thành cũ, và mất một lần bàn giao nghĩa là cái quầy không ai đứng tên.
137. **Phân quyền thay thế** như mọi chỗ dính role cũ: chi nhánh lấy từ nơi nhân viên trực thuộc (chủ cửa hàng không trực thuộc đâu nên phải khai, và không được âm thầm đổi hướng sang chi nhánh khác), còn `cash_drawers:read` vs `read_own` quyết định thấy cả chi nhánh hay chỉ những ca mình làm. Hai cặp quyền này nằm trong catalog từ đợt RBAC mà chưa ai dùng — đây đúng là chỗ chúng sinh ra để dùng.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` · `migrate status` sạch · unit **95/95** · **e2e 33/33 trên Postgres thật**.

Smoke thêm 3 kịch bản chạy trọn một ngày quầy: mở ca → mở ca thứ hai cùng chi nhánh **409** → thu ngân chưa từng giữ quầy không thấy nổi ca (**404**) → `END` trước `START` **409** → `START` hai lần **409** → chốt giữa ca **409** → **bàn giao** sang thu ngân 2 (ca vẫn OPEN, người cũ hết quyền ghi → 403) → thu ngân 2 làm và kết ca → `finalize` (CLOSED, 4 phiếu ca) → `finalize` lần hai **409** → **mở được ca mới sau khi ca cũ đóng** (đúng thứ trước mục 132 làm không được) → `read_own` chỉ thấy ca mình làm, chủ thấy cả hai.

**Một test của tôi sai chứ không phải code**: ban đầu tôi mở ca thứ hai và vẫn giao cho thu ngân 1, rồi lại assert họ chỉ thấy 1 ca — họ thấy 2 là đúng. Đã sửa test cho giao ca mới sang thu ngân 2.

### 2026-08-26, phần 4 — review Cash Drawer rồi sửa

Review nêu 3 lỗi thật, 2 chỗ nới lỏng, 4 đổi response, 1 điểm nhỏ. Chủ dự án chốt: **sửa những lỗi.**

138. **Chốt chống ghi đè của phiếu ca không còn tác dụng khi không bàn giao.** Tôi giữ đúng chốt `updatedAt` của bản cũ nhưng viết `data: dto.nextStaffId ? { currentStaffId } : {}`. **Đo bằng probe chạy thật trên Postgres**: `updateMany` với `data: {}` khớp 1 dòng nhưng **không** bump `@updatedAt` (`matched=1 updatedAtChanged=false`). Nên với `START`, hoặc `END` không bàn giao, chốt không bao giờ tiến — hai request giống hệt (bấm hai lần, client tự retry) đều lọt và ghi **2 phiếu ca trùng**, đúng thứ mà cả module này sinh ra để chặn.
    Bản cũ không dính vì phiếu ca là mảng nhúng: `$push` luôn làm `updatedAt` đổi. Tách sang bảng riêng là đúng cho Postgres nhưng lấy mất tác dụng phụ mà chốt đang dựa vào. Giờ ghi `currentStaffId` **không điều kiện** (giá trị không đổi trừ khi bàn giao), kèm comment giải thích vì sao `data` không được rỗng.
139. **Nhân viên không được phân chi nhánh nhìn thấy quầy của mọi chi nhánh.** `findRow`/`findAll` chỉ lọc chi nhánh khi `user.branchId` có giá trị; một STAFF được cấp `cash_drawers:read` mà chưa phân chi nhánh thì rơi vào "không lọc gì". Bản cũ ném 403 `"User has no branch assigned"`. Đáng nói: **sáng cùng ngày tôi đã xử đúng chỗ này ở `orders.branchScope`** rồi lại để hở ở đây — hai module viết cùng ngày, cùng một luật, lệch nhau.
    Giờ **mọi đường đều đi qua `resolveBranch`**, kể cả đường đọc: có chi nhánh → đúng chi nhánh đó; TENANT_OWNER/ADMIN → chi nhánh họ khai hoặc tất cả; còn lại không có chi nhánh → 403.
140. `resolveBranch` trả `null` cho "mọi chi nhánh" thay vì chuỗi rỗng — `''` vẫn là `string` và sớm muộn lọt vào một `where` như một branchId thật.
141. `shiftLogs` sắp theo `[{loggedAt: asc}, {id: asc}]`. `id` chỉ là tie-break cho **ổn định**, không phải khẳng định thứ tự chèn — hai phiếu cách nhau một micro giây trước đó trả về thứ tự tuỳ mỗi lần đọc, mà `shiftLogs.at(-1)` là nền của toàn bộ máy trạng thái START/END/finalize. Cái thật sự chặn trùng là mục 138.

**Không sửa** (review có nêu, không phải lỗi): `GET /current` lỏng hơn bản cũ (bản cũ chỉ trả ca mình **đang giữ**; giờ ai từng ghi phiếu ca đó cũng xem được — cùng dữ liệu họ đã đọc được qua `GET /:id`, nên không phải rò rỉ); `fromDate`/`toDate` dùng `@IsDateString()` nên nhận cả ISO datetime đầy đủ chứ không chỉ `YYYY-MM-DD`; và 4 điểm đổi response (`businessDate` giờ là ISO datetime — **chỗ frontend dễ vấp nhất**, `shiftLogCount`, limit 20→10, 404→409).

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` · `migrate status` sạch · unit **95/95** · **e2e 35/35 trên Postgres thật**. Smoke thêm 2 kịch bản đúng vào hai lỗi: **hai phiếu START giống hệt gửi song song → 200 + 409, đúng 1 phiếu được ghi**; và nhân viên bị gỡ chi nhánh nhưng vẫn có `cash_drawers:read` → **403** cả khi liệt kê lẫn khi đọc theo id.

### 2026-08-26, phần 5 — review đối chiếu TOÀN BỘ source rồi sửa 4 mục chủ dự án chọn

Review lần này quét cả hai project chứ không riêng module vừa port. Báo cáo đầy đủ (khác biệt cố ý / khác biệt chưa ghi / còn thiếu, kèm bảng endpoint chưa port) nằm ở artifact **"Đối chiếu iKiot-BE và iKiotMS-BE"**. Chủ dự án chọn sửa 4 mục; phần còn lại vẫn để mở trong danh sách dưới.

142. **`/tenants` mở toang cho mọi `TENANT_OWNER`.** Controller sinh tự động chỉ có `@Permissions('tenants', …)`, mà `PermissionsGuard` **short-circuit `TENANT_OWNER`** trước khi kiểm tra gì. Hệ quả: chủ một cửa hàng bất kỳ `GET /tenants` đọc được **toàn bộ tenant trên hệ thống**, `PATCH`/`DELETE` sửa hoặc xoá tenant khác. `TenantService.findAll()` cũng không lọc gì (`where: {}`). `Tenant` là model duy nhất không có `tenantId` của chính nó nên **không có gì để scope** — đúng thứ mà CLAUDE.md đã cảnh báo là phải dùng `AdminOnlyGuard`, và comment ngay trong file cũng đã ghi, chỉ là chưa ai gắn guard. Giờ `@UseGuards(AdminOnlyGuard)` ở cấp controller (guard global chạy trước guard cấp controller nên `request.user` đã có sẵn); giữ nguyên `@Permissions` để `check-permissions.js` vẫn thấy route.
143. **`bankingSepayWebhookApiKey` lộ ra response và cho ghi tự do.** Nặng hơn mục 142 vì nó không chỉ là mật khẩu: `SepayOrderService.findTenantByWebhookKey()` xác định tenant **bằng chính key này**, nên ai đọc được key của tenant khác là chạm được vào webhook thanh toán của họ. Schema Mongoose cũ để `select: false` và chỉ SUPER_ADMIN set qua `PUT /tenant/:tenantId/sepay-key`; Prisma không có `select: false` nên field quay lại mặc định ngay khi port model, và `CreateTenantDto` còn nhận nó từ client. Giờ `TenantService` dùng `TENANT_SELECT` **liệt kê các cột an toàn** (không blacklist cột secret — cột thêm sau phải vô hình cho tới khi có người chủ động mở), áp cho cả `findAll`/`findOne`/`create`/`update`/`remove`. Vẫn ghi được nhưng chỉ qua `/tenants` đã admin-only, tức đúng bằng `PUT /tenant/:tenantId/sepay-key` cũ.
144. **`PATCH /auth/me` không chuẩn hoá email.** Bản cũ làm `String(data.email).toLowerCase().trim()` trước khi check trùng và lưu; pass NestJS đầu tiên bỏ mất. Trong khi đó `firebaseLogin` tra bằng `decoded.email.toLowerCase().trim()` → ai lưu `Foo@Bar.com` là **không đăng nhập Google được nữa**, và check trùng email cũng lọt một dòng chỉ khác hoa/thường. Sửa bằng decorator dùng chung `@NormalizeEmail()` (`src/common/decorators/`) chạy ở tầng `class-transformer`, tức trước cả validation và trước khi DTO tới service — áp cho **cả 4 chỗ ghi `User.email`**: `register`, `PATCH /auth/me`, `POST /users`, `PATCH /users/:id`. Chỉ sửa `/auth/me` thì lỗi vẫn vào được bằng 3 cửa còn lại. Email chỉ để liên hệ (NCC, chi nhánh, kho) cố ý giữ `@IsEmail()` trơn. **Dòng ghi trước hôm nay vẫn có thể hoa/thường lẫn lộn** — còn nợ một `UPDATE users SET email = lower(email)`.
145. **Response envelope `{success, message?, data}` — trả xong, nợ mở từ 17/8.** `ResponseEnvelopeInterceptor` (`APP_INTERCEPTOR`) lo nửa thành công, `AllExceptionsFilter` lo nửa lỗi (exception không bao giờ đi qua interceptor). Điểm đáng nói nhất là **nó merge chứ không lồng** khi handler đã trả object có sẵn `data` hoặc `success`: nhờ vậy `paginate()` ra đúng `{success, data, pagination}` như list cũ, client đọc `body.data[0]`/`body.pagination.total` **không phải sửa gì** — và các handler đang trả `{message, data, ...}` (subscription, leave-balance) hay `{success:true}` trơn (`DELETE /inventory/:id`, `DELETE /customers/:id`) cũng không bị bọc hai lần. Còn lại — entity, mảng, instance của class — vào `data`.
    - Nhánh merge kiểm **prototype** chứ không chỉ `typeof === 'object'`: spread một `Date` hay `Prisma.Decimal` là mất prototype.
    - `@RawResponse()` cho route mà body không thuộc về mình: **2 webhook SePay** (SePay đọc body trả về) và healthcheck `GET /`.
    - Lỗi ra `{success:false, statusCode, message, error?, errors?}`. Lỗi ValidationPipe giữ danh sách theo field ở `errors`, `message` là một câu — đúng shape `{message: "Validation failed", errors}` mà DTO cũ trả; `statusCode`/`error` vẫn giữ nên chỗ nào đang parse shape 3 key của Nest vẫn chạy.
    - **Thứ tự đăng ký trong `AppModule` là load-bearing**: Nest chạy nửa response của interceptor **ngược** thứ tự đăng ký, nên envelope phải đứng **trước** `AuditInterceptor` để bọc **sau**. Đảo lại thì `AuditInterceptor` nhận `{success, data}` thay vì body login nó dùng để nhận diện actor → **mọi lần đăng nhập ghi audit rỗng**.

**Không sửa ở phần 5** (đã nêu trong artifact, chủ dự án chưa chọn): thiếu luồng quên mật khẩu / refresh / logout; thiếu `/tenant/banking` nên **SEPAY đơn hàng chưa dùng được**; 2 cron job (nghỉ phép, ngày lễ) chưa port; `/stats/*`, `/uploads`, `/ai/chat`, `/admin/system-notifications` chưa có. (`sepayTransactionId` và giảm giá `PROMOTION` đã sửa ở phần 6 ngay dưới.)

### 2026-08-26, phần 6 — sửa tiếp 2 mục từ cùng bản review

146. **`sepayTransactionId` không có trong schema.** Cả `Order` lẫn `CashFlow` bên bản cũ đều mang field này và `completeSepayOrder` ghi vào cả hai; port lần đầu bỏ mất, nên một giao dịch đã xác nhận không để lại gì ngoài một dòng `logger.warn`. Đó là **khoá duy nhất** nối một đơn (và dòng tiền của nó) với một dòng trên sao kê ngân hàng. Thêm lại 2 cột, migration `20260826102338_order_sepay_transaction_id`.
    - **`String?` chứ không phải `Number` như model cũ**: payload webhook là JSON không kiểu, và id là định danh chứ không phải số lượng — đúng lựa chọn `SubscriptionInvoice.transactionRef` đã làm từ trước.
    - Controller trước đây trả `''` khi payload dị dạng; giờ trả `null`. `''` nằm trong cột này sẽ đọc thành "có id" với người đi đối soát.
    - Ghi vào **cả hai** bảng như bản cũ: đơn trả lời "đã thanh toán chưa", còn `CashFlow` mới là thứ mà đối soát thật sự đọc.
    - Nửa subscription thì **không thiếu** — `settlePayment` đã ghi `SubscriptionInvoice.transactionRef` từ trước, không đụng.
147. **Giảm giá `PROMOTION` chỉ đúng nếu client tự rải xuống từng dòng.** Đây là mục nặng nhất trong bản review. `grandTotalOf` cố ý không trừ `discountValue` khi `discountType === 'PROMOTION'` với lý do "engine đã rải vào `items[].discountAmount` rồi" — **nhưng đó là giả định về client**, không phải sự thật do máy chủ tạo ra. Một máy bán hàng gửi `appliedPromotions` kèm tổng giảm nhưng để nguyên dòng thì nhận về đơn **nguyên giá, không lỗi gì**: màn hình hiện giảm giá, khách trả đủ tiền. Nghịch lý là chính module này mở đầu bằng câu "tổng đơn được tính, không nhận từ client" — rồi vẫn nhận đúng một con số từ client.
    Giờ `OrderService.priceOrder` chạy chính engine mà `/promotions/calculate` dùng (`PromotionService.calculate`, `OrderModule` import `PromotionModule` — không có vòng lặp, `PromotionModule` không import gì) rồi tự ghi: `discountAmount` từng dòng, `discountType`, `discountValue`, và `promoName`/`discountAmount` của từng dòng `OrderAppliedPromotion`. **Client chỉ gửi `appliedPromotions: [{ promotionId }]`.**
    - Bắt luôn được lỗ thứ hai: engine kiểm lại điều kiện (ngày, chi nhánh, giá trị tối thiểu, hạn lượt, `status`), nên khuyến mãi hết hạn hoặc khác chi nhánh giờ là **400** thay vì được giảm. Kiểm cũ chỉ hỏi "id có tồn tại trong tenant không" — nên id của tenant khác đổi từ **404 → 400** ("không còn tồn tại hoặc không áp dụng cho đơn này"), và một message cho cả hai trường hợp cũng là câu trả lời đúng với id xuyên tenant.
    - **`discountType: 'PROMOTION'` không nhận từ client nữa** — gửi `appliedPromotions` mới là thứ quyết định đơn đó có khuyến mãi. `ORDER` (giảm tay cả đơn) vẫn nhận; trộn cả hai là **400**, vì mỗi đơn chỉ có một cặp `discountType`/`discountValue`, không có chỗ ghi một tổng nửa tay nửa khuyến mãi.
    - `AppliedPromotionDto` bỏ `promoName` và `discountAmount`. Client cũ gửi thừa vẫn chạy — `whitelist: true` cắt key lạ — chỉ là không còn quyết định con số nào.
    - `grandTotalOf` làm tròn **từng dòng trước khi trừ**, đúng cách `pricing-engine.ts` làm tròn `lineTotal`. Cộng chưa làm tròn rồi làm tròn một lần ở cuối khiến đơn có khuyến mãi lệch một hai đồng so với tổng màn hình preview vừa báo — ở quầy thì con số nào đúng cũng thành báo lỗi.
    - Trả giá phải trả: biến thể bị query 2 lần trên đơn có khuyến mãi (một lần ở `priceLines`, một lần trong cart builder của engine). Cố ý — engine giữ cách nhìn giỏ hàng của riêng nó, và thêm một lần đọc theo index cho mỗi đơn có giảm giá là đáng.
    - **Vẫn là hai bước cho việc đếm lượt**: `usedCount`/`PromotionLog` chỉ nhúc nhích khi client gọi `POST /promotions/apply` kèm `orderId` — y như bản cũ. Định giá một đơn không tiêu một lượt, nên client bỏ qua `/apply` thì được giảm mà không tốn hạn mức. Đáng gộp vào transaction tạo đơn, nhưng đó là quyết định cần người chốt nên chưa làm.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` · `migrate status` sạch · unit **104/104** · **e2e 38/38 trên Postgres thật**. Smoke sửa lại kịch bản khuyến mãi để gửi **đúng một `promotionId`** rồi kiểm cả 5 con số máy chủ tự điền, thêm 3 kịch bản: payload kiểu cũ với số bịa (`discountAmount: 999_999`) → vẫn ra 550k; trộn `ORDER` + khuyến mãi → 400; khuyến mãi `INACTIVE` → 400. Cộng thêm kiểm `sepayTransactionId` được ghi vào **cả** `orders` lẫn `cash_flows` sau webhook.

**Kiểm chứng**: `tsc` · `lint` sạch · unit **104/104** (thêm `response-envelope.spec.ts`: merge vs lồng, giữ `success:false` của handler, prototype guard, stream) · **e2e 37/37 trên Postgres thật**. Smoke sửa 107 chỗ đọc body sang `body.data.*` (chỗ đọc `body.data[0]`/`body.pagination` của list **giữ nguyên** — bằng chứng nhánh merge chạy đúng) và thêm 2 test hồi quy: TENANT_OWNER vào `/tenants` (list/read/update/delete) → **403** cả 4; và envelope (list merge, lỗi `success:false` + `errors`, webhook giữ raw).

### 2026-08-26, phần 7 — Redis + phiên đăng nhập + cấu hình cửa hàng (2 mục chặn người dùng)

Chủ dự án chốt: làm nốt phần còn lại, **bám source cũ**, gặp bug thì dừng hỏi. Thứ tự đã thống nhất: Redis+auth → tenant config → cash-flows read-only → uploads → holidays → schedule → attendance → leave → payroll → ticket → stats → AI. Đây là 2 batch đầu — đúng 2 mục "chặn người dùng thật" trong bản review.

**Quyết định đã hỏi**: refresh token dùng **Redis** (không phải bảng Postgres); AI chat **port đầy đủ**; `/cash-flows` **gỡ về read-only**; gặp bug thì **dừng hỏi ngay**.

148. **Redis lên sóng** — `RedisService` (`src/common/redis/`, global) + service `redis` trong docker-compose. Port từ `src/config/redis.js` kể cả phần quan trọng nhất: **Redis chết không làm chết app**. `isReady()` false thì mọi lệnh đọc trả "không có", app vẫn chạy. Lỗi socket log **một lần mỗi lần mất kết nối** chứ không phải mỗi lần retry (cờ `hasPrintedSocketError` của bản cũ). Dùng `SCAN` chứ không `KEYS` — `KEYS` block server cả lượt quét, mà lượt quét đó chạy đúng lúc người ta đang dùng app.
149. **`REDIS_URL` trong `.env` đang trỏ vào Render (Singapore) — production.** Kết nối được thật, đang trống 0 key. Chạy test là ghi refresh token vào đó. Đã hỏi và đổi sang `redis://localhost:6379`, giữ URL Render dưới dạng comment ngay trên để copy sang env deploy.
150. **`RefreshTokenService`** thay collection `RefreshToken` của Mongo. Ánh xạ: 1 document → 1 key `refresh:<userId>:<jti>`; TTL index → key expiry của Redis; `isRevoked: true` → **xoá key** (giữ bia mộ cho một token không dùng được nữa thì để làm gì). `revokeAllFor` = SCAN `refresh:<userId>:*`, thay cho `updateMany({userId})`.
    - **JWT refresh không chứa gì đáng ăn cắp**: `sub` + `jti` ngẫu nhiên + `type: 'refresh'`. Trạng thái thật của phiên là key trong Redis, nên token mà mất key thì không còn là phiên nữa dù chữ ký còn đúng — đó chính là thứ khiến logout/thu hồi có tác dụng **ngay** thay vì chờ hết hạn.
    - **Xoay vòng mỗi lần refresh** (port từ `refreshAccessToken`): token vừa dùng bị xoá, nên dùng lại lần hai là hỏng.
    - `type: 'refresh'` được kiểm khi verify — bản cũ fallback cả hai token về `JWT_SECRET` khi thiếu `REFRESH_TOKEN_SECRET`, nên không có cờ này thì access token replay được thành refresh token.
151. **Thêm 1 điểm chặt hơn bản cũ ở `/auth/refresh`**: đọc lại user và từ chối nếu tài khoản đã bị khoá. Bản cũ chỉ `findById` để ký lại claim vào token. Token phát trước lúc khoá tài khoản không được sống lâu hơn lệnh khoá, mà `JwtStrategy` chỉ canh access token.
152. **OTP sang Redis** kèm fallback in-memory y như file cũ. Fallback là thứ giúp dev chạy không cần container, và **cố ý** không an toàn khi nhiều instance — đúng hành vi cũ. Đồng thời sửa một lỗi của pass port đầu: key giờ đi qua `normalizePhone` (`src/common/utils/phone.ts`). Bản cũ chuẩn hoá, pass đầu thì không, nên mã xin bằng `0912345678` sẽ không verify được khi bước xác nhận gửi `+84912345678` — cùng một số, hai key.
153. **6 route auth còn thiếu đã có**: `/auth/refresh`, `/auth/logout`, `/auth/send-forgot-password-otp`, `/auth/verify-forgot-password-otp`, `/auth/reset-password`, `/auth/check-availability`. Đổi/reset mật khẩu **thu hồi mọi phiên**, đúng bản cũ. Token reset mang `type: 'password_reset'` và được kiểm lại lúc dùng — access token post vào `/auth/reset-password` bị 400.
154. **`/tenant/*` đã port** — `GET|PUT /tenant/me`, `PUT /tenant/banking`, `PUT /tenant/:tenantId/sepay-key`, giữ nguyên đường dẫn số ít của bản cũ. Tách hẳn `TenantSelfService` khỏi `TenantService`: cái này **không bao giờ nhận tenantId từ client**, lấy từ token, nên không có đường nào chạm sang cửa hàng khác. `GET /tenant/me` trả `hasSepayKey: boolean` chứ không trả key — đúng như `getTenant` cũ làm.
    **Đây là thứ mở khoá thanh toán SEPAY**: `OrderService.requireBanking` chặn đơn SEPAY khi chưa có tài khoản ngân hàng, mà trước đó không có endpoint nào để điền.
155. **`notifySystem()`** port từ `systemNotificationService.js` — `Notification` với `tenantId` **và** `recipientId` đều null (chính cặp null đó định nghĩa "noti hệ thống"), broadcast vào room `admin`. Copy nằm ở `templates/tenant.templates.ts` theo đúng rule, không viết thẳng vào service.

**2 bug trong source cũ, đã hỏi và được chốt sửa:**
- **`PUT /tenant/:tenantId/sepay-key` không kiểm quyền theo tenant.** Route chỉ có `authorize('tenants','update')`, handler đọc thẳng `req.params.tenantId`. Comment trong code ghi "SUPER_ADMIN" nhưng không chỗ nào ép — trong khi 2 route admin ngay bên cạnh (`listTenants`, `updateTenantAdmin`) đều check `req.user.role !== 'SUPER_ADMIN'`, nên đây là sót chứ không phải thiết kế. Bất kỳ ai có `tenants:update` ghi được webhook key của cửa hàng khác, mà key đó là thứ định danh tenant ở webhook thanh toán. → gắn `AdminOnlyGuard`.
- **`PUT /tenant/me` cho chủ cửa hàng tự đổi `status` của tenant.** `updateMyTenant` đẩy thẳng `req.body` vào `updateTenant` vốn có `$set: { status }`, nên tenant đang SUSPENDED tự đặt lại ACTIVE được. → bỏ `status` khỏi DTO; status chỉ đi qua `/tenants/:id` (admin).

**Kiểm chứng**: `tsc` · `lint` · `check-permissions` sạch · unit **104/104** · **e2e 42/42 trên Postgres + Redis thật**. Smoke thêm 4 kịch bản auth (xoay vòng + từ chối token đã tiêu; logout; đổi mật khẩu thu hồi **cả hai** phiên; otp→reset token→mật khẩu mới, và access token post vào reset-password bị 400) và chuyển phần seed banking từ ghi thẳng DB sang gọi `PUT /tenant/banking` thật, kèm kiểm `SYSTEM_TENANT_BANK_UPDATED` được sinh ra và `TENANT_OWNER` set sepay-key bị **403**.

### 2026-08-27 — cash-flows read-only + uploads + holidays (batch 3–4)

156. **`/cash-flows` gỡ về read-only** (đã hỏi và chốt ở phần 5). Bản cũ **không có route nào ghi `CashFlow`** — sổ tiền được ghi bởi thứ thật sự làm tiền di chuyển: đơn hoàn tất + tiền thối (`writeSaleCashFlows`), trả hàng, `SupplierService.payDebt`, chốt lương; rồi đọc lại qua các endpoint stats. CRUD sinh tự động thêm `POST/PATCH/DELETE` — tức là cách ghi doanh thu chưa từng xảy ra và xoá doanh thu đã xảy ra. Xoá luôn 2 DTO. `cash_flows:create/update/delete` giờ là cặp catalog không dùng, `check-permissions` liệt kê ra là đúng.
    Thêm `CashFlow` và `Holiday` vào `PORTED_MODELS` của `generate-modules.js` — chạy lại generator sẽ dựng lại CRUD đầy đủ và xoá mất phần sửa tay.
157. **`POST /uploads` đã port** — Cloudinary, folder `ikiot_uploads`, jpg/jpeg/png/webp/gif, 5MB, trả `{url}`. Có auth nhưng **không gate permission**, đúng route cũ (`verifyJwt` trần): URL chỉ có ích khi được ghi lên sản phẩm/hồ sơ, mà mấy chỗ ghi đó đã gate theo resource riêng.
    Khác cách làm (không khác hành vi): bản cũ đẩy thẳng file lên Cloudinary bằng `multer-storage-cloudinary`; ở đây file vào memory rồi `upload_stream`. Bớt 1 dep, và validate nằm ở chỗ trả được 400 tiếng Việt thay vì lỗi của multer. Giới hạn dung lượng chặn **hai lần** có chủ đích: multer ngừng đọc socket, service kiểm lại để lấy message.
158. **Module `holidays` port đầy đủ** — 6 route đúng path cũ, `HolidaySyncService` (Google Calendar) và `HolidayCronService`.
    - `isManuallyEdited` là field cốt lõi: cái gì người sửa thì sync **không bao giờ đè**, kể cả khi họ tắt (`isActive: false`). `skippedManualCount` báo số dòng được để yên. "Sync xoá mất chỉnh sửa của tôi" chính là lỗi mà module này sinh ra để chặn.
    - `isActive` **không** set được qua `PATCH /holidays/:id` — DTO cũ còn trả hẳn lỗi "hãy dùng API /status" chứ không lờ đi. Giữ nguyên.
    - Ngày `YYYY-MM-DD` → UTC midnight; `2026-02-31` qua được regex nên có `isRealCalendarDate` round-trip qua `Date` đúng như DTO cũ.
    - Bản cũ dùng `bulkWrite` upsert; Prisma không có bulk upsert nên loop **trong một transaction** — sync dở dang để lại nửa năm đã cập nhật thì tệ hơn là fail hẳn.
    - Cron `30 2 1 * *` (`Asia/Ho_Chi_Minh`), **năm nay + năm sau** y bản cũ: lương và nghỉ phép lập kế hoạch vắt qua giao thừa nên tháng 12 phải biết trước tháng 1. Không có `GOOGLE_CALENDAR_API_KEY` thì bỏ qua; một tenant lỗi không làm dừng cả lượt.
159. **Lỗ ràng buộc do chuyển Mongo→Postgres** (không phải bug bản cũ, là sót của pass port schema): `@@unique([tenantId, date, branchId, type])` **không ràng buộc gì** với các dòng module này ghi, vì `branch_id` luôn NULL mà Postgres bỏ qua NULL trong unique index — Mongo thì coi null là một giá trị nên index cũ có hiệu lực. Hai lần sync đua nhau, hoặc thêm tay một ngày mà lịch cũng có, đều chèn trùng được. Thêm partial index `WHERE branch_id IS NULL` (migration `20260827020000_holiday_unique_public_per_date`), giữ `@@unique` cũ cho các dòng có branch. Cùng loại với `cash_drawer_sessions_one_open_per_branch` (mục 132).

**Kiểm chứng**: `tsc` · `lint` · `check-permissions` · `migrate status` sạch · unit **104/104** · **e2e 43/43**. Smoke thêm kịch bản holidays: tạo → trùng ngày **409** (bằng chứng partial index chạy) → ngày không có thật **400** → `isActive` qua PATCH bị **400** → đổi qua `/status` → lọc theo `year` → xoá → thao tác tiếp **404**.

### 2026-08-27, phần 2 — shift-templates + working-schedules + quyền trưởng ca (batch 5)

**2 quyết định đã hỏi**: bỏ tầng dedup, thay bằng unique index; và **port đầy đủ** `managedScheduleAccess`.

160. **`shift-templates` port xong.** 5 route đúng path cũ. Giờ đổi từ chuỗi `"08:00"` sang `@db.Time`; `shift-time.ts` là chỗ duy nhất map qua lại, và **toàn bộ dùng UTC có chủ đích**: Prisma trả cột `time` thành `Date` ngày 1970-01-01 theo UTC, đọc bằng `getHours()` là lệch cả template đúng bằng offset (ở VN thành 15:00).
    **Ca mẫu được phép có giờ kết thúc sớm hơn giờ bắt đầu** — 22:00–06:00 là ca đêm. DTO cũ có viết check này rồi **comment lại** kèm ghi chú `IGNORE END TIME MUST BE AFTER START TIME`, và `shiftInterval()` đọc đúng trường hợp đó để đẩy `endAt` sang hôm sau. Hai thứ là hai nửa của một quyết định, sửa một nửa là sinh ra ca có độ dài âm.
161. **`working-schedules` port xong** — 10 route đúng path và đúng permission cũ (kể cả 4 action riêng `read_all`/`readBR`/`readWH`/`read_own`). Thứ tự route quan trọng: `current`, `me`, `branches`, `warehouses` phải khai trước `:id`.
    - Phân ca hàng loạt **gộp theo slot** (template/ngày/loại/giờ) — trong bộ nhớ trước, rồi gộp tiếp với cái đã có trong DB. Đó là thứ khiến "thêm Bình vào ca sáng thứ Ba" chạy được khi ca sáng thứ Ba đã có người.
    - `08:00` nghĩa là 08:00 **giờ Việt Nam**: `schedule-time.ts` hardcode +07:00 y như `WorkingScheduleDateUtils` cũ. Đúng cho VN (không có DST từ 1975), sai về tổng quát — giữ vì đổi sang timezone database là âm thầm đổi ý nghĩa của mọi `startAt` đã lưu.
    - Sửa/xoá ca bị từ chối khi đã có chấm công: dòng attendance tính đi muộn từ đúng cái `startAt` đó, dời ca dưới chân nó là viết lại lịch sử. Gỡ người cuối cùng khỏi ca thì xoá luôn ca — y bản cũ.
162. **Bỏ `deduplicateWorkingSchedules`** (~150 dòng). Comment của chính nó thừa nhận "chỉ chuẩn hoá response, không sửa DB" — nó tồn tại vì Mongo không ràng buộc gì và dữ liệu thật đã mọc ra bản ghi trùng. Postgres cấm được: migration `20260827030000_working_schedule_unique_slot` (partial — CANCELLED/DELETED không được chặn việc xếp lại đúng slot đó), cộng `@@id([scheduleId, userId])` của bảng join và `@@unique([userId, scheduleId])` của Attendance.
    Bỏ nó cũng gỡ luôn lý do bản cũ **phân trang trong bộ nhớ**: phải nạp hết mọi lịch khớp filter mới dedupe được. Giờ phân trang lại là `LIMIT`.
163. **`validateRoleHierarchy` bỏ** cùng với các role nó so sánh — xếp ca là `schedules:create`. Phần còn sống: "mọi người được xếp ca phải là STAFF đang ACTIVE trong tenant".
164. **Quyền trưởng ca (`managedScheduleAccess`) đã port đầy đủ.** Ai là `managedBy` của một ca **đang chạy** thì được thêm một tập quyền cố định trong đúng thời gian ca đó.
    - `ShiftSupervisorService.resolve()` chạy trong `JwtStrategy` mỗi request — cùng lý do với việc đọc lại quyền của Role: nó phải phản ánh **đồng hồ của chính request này**, ca kết thúc 2 phút trước không được cấp gì nữa. Một query có index, và short-circuit `null` ngay với ai không phải STAFF.
    - **Ba thứ giữ nó hẹp, port đủ cả ba**: chỉ ca `SCHEDULED` có khung giờ chứa `now` (hết hạn theo đồng hồ, không có bước thu hồi nào để quên); chỉ các cặp trong `TEMPORARY_PERMISSIONS` (tập cố định do code sở hữu, không bao giờ cấp được nhiều hơn một `Role` có thể cấp); và chỉ địa điểm **vừa** nằm trong ca **vừa** là nơi chính trưởng ca được phân. `suppliers` bỏ qua vế địa điểm vì hồ sơ NCC là cấp tenant — bản cũ cũng ngoại lệ đúng chỗ đó.
    - Hai nửa và cần cả hai: `AuthUser.permissions` thêm key (để `PermissionsGuard` cho qua), `AuthUser.shiftSupervision` mang địa điểm. `supervisesLocation()` chính là cái seam mà `StockMovementService.canActAt()` và `CashDrawerSessionService.resolveBranch()` nới ra — đúng như CLAUDE.md đã hứa từ lúc port stock-movement.

**Kiểm chứng**: `tsc` · `lint` · `check-permissions` · `migrate status` sạch · unit **104/104** · **e2e 45/45**. Smoke thêm 2 kịch bản: xếp ca (gộp 2 assignment cùng slot thành 1 lịch, ca đêm đẩy `endAt` sang hôm sau, `startAt` là 01:00 UTC ⇔ 08:00 VN, trùng ca **409**, filter theo khoảng ngày + `dayInfo` + tóm tắt chấm công, gỡ 1 người khỏi ca); và quyền trưởng ca (`stock_movement:read` **403 → 200 → 403** trên **cùng một access token**, chỉ do dời khung giờ của ca).

### 2026-08-27, phần 3 — attendance (batch 6)

165. **Module `attendances` port đầy đủ** — 7 route đúng path và permission cũ. Gộp `TakeAttendanceService` + `ManageAttendanceService` làm một vì hai cái dùng chung mọi luật.
    - `workDate` **chép từ schedule**, không suy từ thời điểm check-in — ca sáng sớm hoặc ca qua đêm sẽ rơi sai ngày, mà payroll gom theo đúng ngày đó. Bản cũ có comment y hệt ở đúng dòng đó.
    - **Geofence có hai kiểu lỗi khác nhau và không được gộp** (`geo-fence.ts`, hàm thuần, có unit test): `422 LOW_ACCURACY` khi fix GPS quá mờ để phán (lỗi của máy, thử lại được) và `403 OUT_OF_RANGE` khi fix đủ tốt và nói người đó đang ở chỗ khác. Kiểm accuracy **trước** cũng vì lý do đó: fix mờ mà rơi ra xa là fix hỏng, không phải nhân viên ở xa. Gộp lại là bảo người đang đứng trong cửa hàng rằng họ không được phép ở đó.
    - Check-in mở sớm 30 phút và đóng khi ca kết thúc. Check-in sau khi ca xong không phải "đi muộn" — phải qua quản lý kèm lý do (`POST /attendances/manual`).
    - Quản lý **không bao giờ** sửa được chấm công của chính mình, dù có quyền gì: ý nghĩa của việc ghi lại ai sửa chính là người sửa không phải người được trả tiền cho số giờ đó.
    - `assertPayrollPeriodOpen` port từ `PayrollAttendancePolicy`: REVIEW trở đi thì đóng băng input, DRAFT vẫn sửa được.
    - Thay thế phân quyền: của mình thì luôn xem được; `attendances:read` mở rộng ra địa điểm của mình; ghi của người khác cần `attendances:update` + cùng địa điểm. Việc thay thế này tiện thể sửa một bất đối xứng cũ — `assertCanManuallyCheckout` cho TENANT_OWNER và BRANCH_MANAGER nhưng **không** cho WAREHOUSE_MANAGER, nên nhân viên kho không có ai ngoài chủ cửa hàng sửa chấm công hộ được.
166. **Bug bản cũ, đã hỏi và chốt cách xử** — `GET /attendances` có `?lateOnly=true` và `?overtimeOnly=true`, lọc theo `lateMinutes > 0` và `overtimeMinute > 0`, nhưng **không chỗ nào trong toàn bộ source cũ ghi hai cột đó**. Hai filter luôn trả rỗng, hai field luôn null.
    - **`lateMinutes` giờ được tính và lưu lúc check-in**, bằng đúng `lateMinutesOf` (grace all-or-nothing) mà schedule view và payroll đang dùng — nên giá trị lưu và giá trị suy ra khớp nhau, và cơ chế "ưu tiên đã lưu, không có thì suy ra" của payroll vẫn chạy với dữ liệu cũ.
    - **`overtimeOnly` gỡ bỏ chứ không sửa**: khác với đi muộn, "overtime" **không có định nghĩa nào trong bản cũ** để khôi phục — giờ làm thêm được xếp thành ca loại OVERTIME chứ không cộng dồn lên dòng chấm công. Bịa ra một định nghĩa cho cột đó là bịa ra một luật tính lương.

**Kiểm chứng**: `tsc` · `lint` · `check-permissions` sạch · unit **113/113** (thêm `geo-fence.spec.ts`: khoảng cách Haversine, 422 vs 403, thứ tự ưu tiên accuracy, mặc định 100m) · **e2e 46/46**. Smoke thêm kịch bản chấm công đầy đủ: đứng xa 330m → **403 OUT_OF_RANGE**; đứng đúng chỗ nhưng accuracy 500m → **422 LOW_ACCURACY**; check-in hợp lệ → `lateMinutes` **60** được lưu (grace 15); check-in lần hai → **409**; check-out → `workedMinutes` 30; `?lateOnly=true` trả **1 dòng** (trước đây luôn rỗng); tự sửa chấm công của mình → **403**.

### 2026-08-27, phần 4 — leave requests (batch 7)

167. **Module `leave-requests` port đầy đủ** — 12 route đúng path và permission cũ (kể cả 8 action riêng `read_mine`/`read_all`/`readBR`/`readWH`/`cancel`/`approve`/`reject`/`create_emergency`). `POST /leave-requests` **không gate permission**, đúng route cũ: ai cũng được nộp đơn cho chính mình; thứ cần quyền là đọc hoặc quyết định đơn của người khác.
    - **Duyệt là tiêu quỹ phép**: `paidLeaveDays` trừ vào `leaveBalanceRemainingDays` trong cùng transaction với đổi trạng thái và bàn giao, bằng `updateMany` có điều kiện — hai lượt duyệt đồng thời không rút âm được, vì "không khớp dòng nào" chính là câu trả lời cho "còn đủ không". Mặc định 12 ngày (mức tối thiểu luật định) và điền vào lần dùng đầu, y bản cũ.
    - **"Quản lý" là thay thế, và sắc hơn bản cũ**: bản cũ hỏi `role in (BRANCH_MANAGER, WAREHOUSE_MANAGER)` ở 4 chỗ để quyết có cần bàn giao không. Vai trò đó không còn, nên câu hỏi thành đúng thứ đang thật sự được hỏi: **người này có quản lý ca nào trong khoảng nghỉ không?** Quản lý chi nhánh mà không có ca nào trong khoảng đó thì bản cũ cũng đâu cần bàn giao — nên đây chính xác hơn chứ không lỏng hơn.
    - Không ai tự duyệt đơn của mình; từ chối bắt buộc có `reviewNote`; huỷ đơn bị từ chối khi ngày nghỉ đã bắt đầu (lúc đó lịch đã xếp quanh nó và có người đã trực thay ngày đầu).
    - `leave-date.ts` **cố ý tách khỏi** `working-schedules/schedule-time.ts`: bên kia quy đổi *giờ* theo offset VN vì ca bắt đầu ở một giờ đồng hồ cụ thể; ngày nghỉ không có giờ, áp offset vào là dịch mốc và làm một ngày nghỉ trải ra hai ngày lịch.
    - **`deleteLeaveRequest` không port**: có trong service cũ nhưng **không route nào gọi tới**, và nó không hoàn quỹ phép lẫn không gỡ bàn giao.
168. **Bug bản cũ, đã hỏi và chốt sửa** — `cancelLeaveRequest` trả lịch bàn giao về `managedBy: null` thay vì về quản lý cũ. Duyệt đơn thì ca chuyển từ `userId` → `handoverToUserId`; huỷ đơn thì phép nghịch đảo đúng phải là chuyển ngược lại, không phải xoá trắng. Hệ quả cũ: sau khi huỷ, những ca đó **không còn ai quản lý**, và từ batch 5 trở đi còn mất luôn quyền trưởng ca của cả hai người. Giờ trả về `leaveRequest.userId`.
169. **`NotificationService` port xong hoàn toàn** — thêm `approversOf()` và `displayName()`, hai hàm cuối còn nợ từ 17/8. `approversOf` đọc quản lý được bổ nhiệm của địa điểm (fallback về chủ cửa hàng) và **luôn loại người nộp đơn ra**, nên quản lý nộp đơn của chính mình không bao giờ thành người duyệt của chính mình — `review` còn chặn tự duyệt lần hai.
170. **Cron nghỉ phép đã chạy lại** (`leaveRequestJob.js`): 00:01 giờ VN, EXPIRE các đơn PENDING đã quá `startDate`. Nạp danh sách **trước** `updateMany` vì sau đó không còn biết dòng nào vừa đổi mà báo cho chủ đơn. Giữ lại ghi chú của bản cũ về biểu thức cron: bản trước đó viết `"0 1 0 1 * *"` 6 trường nên trường thứ 4 bị đọc là **ngày trong tháng**, job chỉ chạy mùng 1 — trái với chính comment của nó.

**Kiểm chứng**: `tsc` · `lint` · `check-permissions` sạch · unit **113/113** · **e2e 47/47**. Smoke thêm kịch bản nghỉ phép đủ vòng: preview bàn giao thấy 1 ca; nộp đơn **không** chọn người nhận bàn giao → **400**; nộp trùng ngày → **409**; tự duyệt → **403**; duyệt 4 ngày trong khi xin 3 → **400**; duyệt 2 có lương + 1 không lương → quỹ 12→**10**, ca chuyển sang người nhận; huỷ → quỹ về **12** và ca **trả lại đúng người nộp đơn** (bản cũ trả về null).

### 2026-08-27, phần 5 — payroll (batch 8), mắt xích cuối của chuỗi HR

Module lớn nhất còn lại: `PayrollService.js` 58KB + `PayrollDayRateCalculator` + `PaySheetService` + `PayrollSettingService`. Port thành **4 module** ở đúng path cũ, **13 route**.

171. **Tách ba tầng, và việc tách chính là điểm mấu chốt.** `payroll-math.ts` giữ mọi luật quyết định tiền dưới dạng **hàm thuần không chạm DB** (33 unit test) — cùng hình dạng `pricing-engine.ts`; `PayslipBuilderService` nạp dữ liệu và áp luật; `PayrollPeriodService` ghi và đẩy kỳ lương qua vòng đời. Bản cũ gộp cả ba trong một file 1500 dòng.
172. **Luật hoàn phút công tồn tại để không phạt hai lần.** Có rule phạt đi muộn thì *toàn bộ* phần công hụt được hoàn lại vì tiền đã bị lấy bằng tiền phạt; không có rule thì chỉ hoàn phần trong grace. Đây là lý do deductions **phải đọc trước khi định giá ca** — thứ tự này không tuỳ tiện.
173. **Ngày lễ đè lên cuối tuần**, không cộng dồn. Chỉ `PUBLIC_HOLIDAY` có hệ số; `COMPANY_HOLIDAY` là ngày nghỉ riêng của cửa hàng, trả hệ số luật định cho nó là bịa ra tiền.
174. **Ca NORMAL của nhân viên `FIXED` sinh line trị giá 0** — lương kỳ prorate trên cả kỳ, chặn tối đa 1 công/ngày. Tính theo từng ca thì không chặn được khi người ta làm hai ca một ngày. Và không bao giờ vượt `salaryPerPeriod`.
175. **Đi làm thì thắng.** Vẫn chấm công vào ngày đã xin nghỉ thì giờ thực làm đã được trả; cộng tiền phép hoặc trừ nghỉ không lương nữa là tính hai lần cùng một ngày. Dòng vẫn ghi kèm `ignoredBecauseAttended: true` để payslip giải thích được.
176. **Phép phân bổ từ đầu đơn chứ không từ đầu kỳ**, và chỉ vào ngày có xếp ca. Nên `gather()` nạp lịch theo **toàn bộ khoảng của mọi đơn nghỉ** — nạp mỗi lịch trong kỳ thì đơn vắt qua hai kỳ sẽ khởi động lại phân bổ và trả tiền phép hai lần.
177. **`MARK_PAID` ghi `CashFlow` và đổi trạng thái trong cùng transaction** — kỳ lương không bao giờ hiện PAID mà thiếu dòng sổ chứng minh. `CashFlow.payrollPeriodId` unique nên replay không ghi dòng thứ hai. Tổng chi **đọc lại bên trong transaction đó**: con số từ response list là câu trả lời của request khác và có thể đã cũ.
178. **`paymentMethod` cứng CASH** (đã hỏi và chốt). DTO **không nhận** field này — bản cũ nhận rồi từ chối mọi giá trị khác, lý do ghi trong `PayrollActionDto`: dán nhãn BANK_TRANSFER/SEPAY cho một khoản chi là đánh dấu tiền đã đi qua tích hợp mà nó chưa hề chạm. 4 giá trị còn lại trong schema là không tới được.
179. **`staleDraftPeriods` gom 1 query cho cả trang** (đã hỏi và chốt) — bản cũ chạy 1 query/kỳ, danh sách 20 kỳ là 20 round-trip. Chỉ kỳ DRAFT mới hỏng được: từ REVIEW trở đi chấm công đã bị `assertPayrollPeriodOpen` đóng băng.
180. **Sửa payslip nháp tính lại từ các thành phần đã lưu**, không từ `netSalary` trước đó, nên sửa hai lần không cộng dồn. Chỉ DRAFT.
181. **`periodStartDay` không được tôn trọng — và đó không phải bug.** `buildMonthlyPeriodRange` cũ nhận tham số rồi bỏ qua, kỳ lương luôn là trọn tháng dương lịch. Nhưng **cả hai DTO cũ đều chủ động từ chối** field này ("Không hỗ trợ ngày bắt đầu kỳ lương tùy chỉnh") — tính năng cố ý chưa làm, chặn nhất quán ở tầng input; chỉ có một comment ở `generatePayrollPeriod` bị lạc hậu. Bỏ khỏi DTO mới, ghi rõ lý do.
182. **Thêm một chốt sớm ở `paysheets`**: mức lương ứng với `payType` phải > 0 ngay lúc tạo. Bản cũ nhận paysheet FIXED thiếu `salaryPerPeriod` rồi mới từ chối lúc sinh bảng lương, dưới dạng lý do "skipped" từng nhân viên. Chốt cũ vẫn giữ (paysheet sửa được sau khi đã gắn người) nhưng cửa hàng biết ngay lúc cấu hình. Cũng **bỏ lọc theo `createdBy`** của `buildPaySheetFilter` — vai trò đó không còn, và giấu paysheet của đồng nghiệp chỉ tạo ra bản trùng.
183. `@db.Date` cho `periodStart`/`periodEnd` xoá luôn màn `buildPeriodDate` cộng trừ 7 tiếng (Mongo không có kiểu date nên phải lưu instant nửa đêm giờ VN, kèm cảnh báo tuyệt đối không `.toISOString().slice(0,10)` giá trị đã lưu). Chỉ `assertPeriodHasEnded` còn cần offset — "kỳ đã kết thúc chưa" là câu hỏi về ngày địa phương của cửa hàng.

**Kiểm chứng**: `tsc` · `lint` · `check-permissions` sạch · unit **146/146** (thêm 33 test `payroll-math.spec.ts`) · **e2e 48/48**. Smoke chạy trọn vòng đời: preview (đi muộn 40' được hoàn vào thời gian, bị tính 50k một lần → 400k − 50k = **350k**) → tháng chưa kết thúc **422** → tạo DRAFT → trùng kỳ **409** → ứng lương −100k → **250k**, sửa lại lần hai **không cộng dồn** → điều chỉnh làm âm **422** → approve khi đang DRAFT **409** → submit → return-to-draft thiếu lý do **400** → approve → mark-paid ra **PAID** + đúng **1 dòng PAYR 250k EXPENSE**, `paymentMethod` = CASH do server ghi → nhân viên thấy phiếu qua `/my-payslips`.

### 2026-08-27, phần 6 — ticket hỗ trợ (batch 9)

Port trọn `src/modules/ticket` của bản cũ: 8 route giữ nguyên đường dẫn (5 dưới `/tickets` cho cửa hàng, 3 dưới `/admin/tickets` cho vận hành), một `TicketService` chung cho cả hai phía.

184. **Ticket thuộc về cửa hàng, không thuộc về người mở nó.** `GET /tickets/my` mang tên "của tôi" nhưng lọc theo `tenantId` — đồng nghiệp đọc và trả lời được cùng thread. Đây là hành vi bản cũ và nó **cố ý**: cuộc hội thoại hỗ trợ sống lâu hơn người mở nó. Đổi sang lọc theo `userId` sẽ làm mồ côi mọi thread khi nhân viên nghỉ việc.
185. **`GET /:id` và `DELETE /:id` dùng chung cho cả hai console.** Router cũ mỗi route đúng một handler, kiểm tra sở hữu nằm *bên trong* chứ không tách bản `/admin` song song — nên một cái link tới ticket mở ra như nhau bất kể ai bấm. `assertCanReach` chính là phép kiểm đó: vận hành tới được mọi thread, người khác chỉ tới được của cửa hàng mình.
186. **`isDeletedByTenant` là kiểu xoá duy nhất.** Không có gì bị xoá thật. Cờ này giấu thread khỏi cửa hàng còn danh sách admin — **cố ý không lọc** — vẫn hiện kèm badge "đã xoá". Bản cũ set cờ *bất kể ai xoá*, nghĩa là vận hành xoá thì cửa hàng cũng mất thread; đã hỏi và **giữ nguyên hành vi cũ** (câu hỏi không được chọn phương án nào → port trung thành).
187. **Trạng thái là hệ quả của việc ai vừa nói**, client không bao giờ gửi lên: cửa hàng trả lời → ép `OPEN` (cần người trực lại), vận hành trả lời → ép `IN_PROGRESS`, và `PATCH /admin/tickets/:id/close` là thứ duy nhất ghi `CLOSED`. Trả lời thread đã đóng là 400. `RESOLVED` có trong enum và **chưa route nào từng ghi** — giữ lại vì đó là dữ liệu production, không phải rác cần dọn.
188. **Mỗi lần trả lời đều ghi lại status kể cả khi không đổi**, để `@updatedAt` nhảy và thread nổi lên đầu cả hai danh sách. Mongo `ticket.save()` sau khi push vào mảng nhúng `messages[]` làm việc đó ngầm; ở đây `messages` tách thành bảng con nên chỉ insert con thôi thì **bản ghi cha không đổi** — sắp xếp theo `updatedAt desc` sẽ sai. Cùng loại bẫy với các chỗ khác khi mảng nhúng bị tách bảng.
189. **Hai phía báo tin bất đối xứng, và đó là chủ ý.** Mở ticket ghi **system notification** (`tenantId`/`recipientId` đều null) vì vận hành ngồi sẵn với danh sách ticket; vận hành trả lời thì đẩy vào inbox riêng của chủ cửa hàng, vì họ gửi xong là quay lại bán hàng — socket event chỉ tới nếu họ đang mở app. Copy nằm ở `templates/ticket.templates.ts`.
190. **`ticketId` đổi sang `generateReference(REFERENCE_PREFIX.TICKET)`** (đã hỏi và chốt). Scheme cũ `TK-<6 số cuối epoch ms><2 số ngẫu nhiên>` chạy trên cột `@unique`: 6 số đó lặp lại mỗi ~16.7 phút, chỉ có 90 hậu tố — trùng là ném P2002 thẳng ra 500. Giờ dùng đúng generator của mọi mã tham chiếu khác (ORD/SUP/PAYR/IKMS), 2^40 khả năng. **Format hiển thị đổi**: `TK-12345678` → `TKA1B2C3D4E5`.
191. **`senderName` trong bản cũ luôn là số điện thoại.** Controller viết `profile.firstName + lastName`, fallback email rồi phone — nhưng payload JWT cũ **không mang `profile` cũng không mang `email`**, nên mọi tin nhắn trong production đều bị đóng dấu số điện thoại. Biểu thức được port đúng nguyên văn; nó chạy đúng ở bản mới chỉ vì `JwtStrategy` đọc lại user mỗi request.
192. **Đây là module lộ nhất trong bản cũ**: cả 8 route chạy trên `verifyJwt` trần, **không có một lời gọi `authorize()` nào** — bất kỳ tài khoản đã đăng nhập nào cũng đọc hoặc xoá mềm được ticket của cửa hàng khác nếu đoán được id. Giờ gate theo resource `tickets` trong catalog; 3 route admin thay phép thử `role === 'SUPER_ADMIN'` viết tay trong từng handler bằng `AdminOnlyGuard`, và **không mang `@Permissions`** (admin nền tảng không có role cấp cửa hàng để đối chiếu).
193. **Không thêm bộ lọc `status` cho danh sách admin** dù rất tiện — bản cũ `Ticket.find()` không lọc gì. Enhance thì phải hỏi, mà thứ này không chặn ai, để lần sau.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **146/146** · **e2e 49/49**. Smoke đi trọn một thread: cửa hàng mở (mã `TK…`, status OPEN, 1 tin) → có `SYSTEM_TICKET_CREATED` với `tenantId`/`recipientId` đều null → `/admin/tickets` bằng token TENANT_OWNER **403** → admin trả lời → `IN_PROGRESS`, 2 tin, `TICKET_REPLIED` vào inbox chủ shop → cửa hàng trả lời → về `OPEN`, 3 tin → admin close → trả lời tiếp **400** → cửa hàng xoá → biến khỏi `/tickets/my` nhưng **vẫn còn** trong `/admin/tickets`.

### 2026-08-27, phần 7 — stats / dashboard (batch 10)

Port trọn `src/modules/stats`: 9 route giữ nguyên đường dẫn (8 dưới `/stats` cho cửa hàng, 1 `/stats/admin/overview` cho vận hành). Module mới hoàn toàn — trước đây chưa có gì.

194. **Hai dashboard, hai sổ tiền khác nhau, lẫn là hỏng nặng.** Doanh thu của cửa hàng là `Order`/`CashFlow` — tiền thu tại quầy. Doanh thu của vận hành là `SubscriptionInvoice` — tiền cửa hàng trả iKiot để mua gói, vào tài khoản ngân hàng khác. Không có gì bắc cầu giữa hai bên.
195. **Phạm vi xem quyết định bởi nơi được phân công, không phải bởi `?branchId=`** (`stats-scope.ts`). Bản cũ rẽ theo `BRANCH_MANAGER`/`WAREHOUSE_MANAGER`; ai khác có `reports:read` — kể cả thu ngân — **thấy toàn bộ cửa hàng**. Hai vai trò đó không còn, và posting đã thay thế chúng ở mọi module đã port trước đó, nên áp ở đây luôn. Truyền id địa điểm **của người khác** giờ là 403, chỗ bản cũ lặng lẽ bỏ qua và hiện số của chính bạn dưới cái tiêu đề của chi nhánh khác — kiểu sai nguy hiểm vì người xem không biết mình đang xem nhầm.
196. **Posting ở kho thì không có doanh số để xem.** Bản cũ viết `{ branchId: null }` cho WAREHOUSE_MANAGER — một filter bất khả thi vì đơn nào cũng có chi nhánh. Giữ nguyên kết quả (rỗng) nhưng viết đúng ý nghĩa: `impossible: true`, trả mảng rỗng mà không cần đi xuống DB. Báo doanh số của **cả cửa hàng** cho một người ở kho còn tệ hơn nhiều so với báo 0.
197. **`toDate` dạng `YYYY-MM-DD` phải là hết ngày, không phải nửa đêm.** Cắt về nửa đêm là âm thầm mất trọn một ngày doanh thu. Cả hai đầu nhận `YYYY-MM-DD` (hiểu là ngày **địa phương**) hoặc ISO đầy đủ.
198. **`changePct` trả `null` khi kỳ trước bằng 0**, không phải `0` và không phải `Infinity`. "Từ không lên có" không có phần trăm trung thực nào, còn `0` hiển thị trên dashboard thành "đi ngang" — đúng ngược lại sự thật. Client render dấu gạch.
199. **`previousPeriod` dài đúng bằng kỳ đang xem và kết thúc trước 1 ms.** Cả hai đầu đều inclusive nên cả hai cửa sổ rộng `span + 1` ms; so 30 ngày với 29 ngày thì mọi phần trăm đều sai một cách khó phát hiện. Mấy dấu `- 1` không phải trang trí.
200. **Toàn bộ phép toán nằm ở `stats-math.ts` dạng hàm thuần + 16 unit test** — cùng hình dạng `pricing-engine.ts`/`payroll-math.ts`.
201. **Bốn câu trả lời phải viết `$queryRaw`** vì Prisma không diễn đạt được: gom bucket theo ngày địa phương (`to_char(created_at AT TIME ZONE 'Asia/Ho_Chi_Minh', …)`), doanh thu dòng (`SUM(unit_price * quantity - discount_amount)`), và định giá tồn (`SUM(stock * cost_price)` qua join). Tính bằng JS nghĩa là kéo **mọi dòng đơn hàng trong kỳ** lên RAM để cộng. Còn lại dùng `groupBy`/`aggregate` của Prisma. Mọi tham số đi qua bind của tagged template; ngoại lệ duy nhất là cột `ORDER BY` của `getTopProducts` — Postgres không nhận bind ở đó, nên chọn từ whitelist 2 giá trị, không bao giờ từ query string.
202. **Timezone trong bucket là thứ mang tải.** Đơn bấm lúc 23:30 thuộc doanh thu **ngày hôm đó**; gom theo timestamp UTC thô sẽ đẩy nó sang hôm sau với mọi cửa hàng ở Việt Nam. Mongo làm việc này bằng tham số `timezone` của `$dateToString`.
203. **`customerCount` đếm distinct `customerId`, và mọi đơn khách lẻ trỏ về đúng một row `KH_VANGLAI`** nên gộp thành 1 "khách". Bản cũ `$addToSet` một `customerId` null và ra đúng con số ấy — port không làm đổi số trên dashboard. Không phải bug cần sửa, chỉ cần biết khi đọc.
204. **`lowStockThreshold` là tham số phẳng trên query, cố ý KHÔNG dùng `Inventory.minStock`.** Hai thứ trả lời hai câu khác nhau: `minStock` là "món này cần nhập thêm", còn ngưỡng ở đây là thanh trượt "cho tôi xem mọi thứ dưới N". Endpoint cũ chỉ có thanh trượt.
205. **`flow=ORD` không đồng nghĩa với "thu".** Đơn hoàn trả ghi một dòng EXPENSE cũng mang tiền tố ORD — e2e ban đầu assert `expense === 0` và fail, đúng như thiết kế. `ORD` và `PAYR` chia đôi phía chi.
206. **Tách `StatsQueryDTO` cũ thành 6 DTO theo route.** Một DTO dùng chung cho cả 9 route là lý do `?sortBy=` im lặng không làm gì trên `/stats/overview`; giờ mỗi route chỉ khai báo tham số nó thật sự đọc, và Swagger nói đúng.
207. `/stats/admin/overview` cũng là route `verifyJwt` trần với `role === 'SUPER_ADMIN'` viết tay trong handler → `AdminOnlyGuard`, không mang `@Permissions` (admin nền tảng không có role cấp cửa hàng).

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **162/162** (thêm 16 test `stats-math.spec.ts`) · **e2e 52/52**. Smoke chạy dashboard trên dữ liệu thật mà chính suite đó vừa bán ra: các bucket ngày địa phương **cộng lại đúng bằng** tổng của `/stats/overview`, `revenue-by-staff` cũng vậy; `top-products` xếp hạng giảm dần theo doanh thu tính trong SQL; `flow=ORD` + `flow=PAYR` chia trọn phía chi; `/stats/inventory` định giá qua join `cost_price` và mỗi dòng low-stock có **đúng một** trong `branchId`/`warehouseId`; nhân viên không có `reports:read` → 403, có rồi thì xem được chi nhánh mình (200) nhưng chi nhánh khác **403**; `TENANT_OWNER` gọi `/stats/admin/overview` → 403.

### 2026-08-27, phần 8 — console thông báo của vận hành (batch 11)

Port trọn `src/modules/system-notification`: 7 route giữ nguyên đường dẫn, đặt trong module `notifications` vì chúng đọc **cùng một bảng** với inbox của cửa hàng. Kèm `EmailService.sendSystemNotificationEmail` (nợ mở từ đợt subscription).

208. **`tenantId` và `recipientId` cùng null là thứ định nghĩa "thông báo hệ thống".** Mọi query trong `AdminNotificationService` đều mang cặp đó (`systemFilter`), cộng thêm whitelist type `SYSTEM_*` như cổng thứ hai hẹp hơn — để mai kia thêm một type `notifySystem()` mới thì màn hình vận hành **không tự đổi** cho tới khi có người thêm nó vào danh sách một cách cố ý.
209. **`PATCH /admin/system-notifications/:id/read` bản cũ không lọc gì cả** (đã hỏi và chốt) — đúng một trong năm handler quên. Vận hành PATCH bất kỳ id nào cũng đổi được cờ `isRead`, kể cả thông báo gửi riêng cho một nhân viên của một cửa hàng, **và đọc được nội dung** qua response. Giờ dùng đúng filter mà các route delete đã có sẵn, không khớp thì 404. E2E ghim lại: lấy một row `TICKET_REPLIED` của cửa hàng rồi PATCH/DELETE qua route admin → 404 cả hai, cờ `isRead` không đổi.
210. **Announcement không phải sự kiện hệ thống.** `POST /admin/notifications` là vận hành **viết** email cho chủ cửa hàng; lưu cùng bảng, cùng cặp null, nhưng type riêng `ANNOUNCEMENT` và **không bao giờ** xuất hiện trong feed sự kiện. `targetTenants` là bảng nối `NotificationTargetTenant`, chỉ ghi khi `targetType = SELECTION` — một announcement `ALL` mà vẫn kèm danh sách sẽ để lại bản ghi khai báo phạm vi hẹp hơn thực tế.
211. **Gửi mail fire-and-forget và `EmailService` tự nuốt lỗi của nó.** Một địa chỉ hỏng không được làm hỏng cả lô, và vận hành viết cho toàn bộ cửa hàng cả nước không thể ngồi chờ vài trăm lượt SMTP. Bản ghi được ghi **trước**, nên lịch sử còn nguyên kể cả khi gửi thất bại. Caller không `await` nên nếu hàm ném thì thành unhandled rejection — đó là lý do `try/catch` nằm trong `EmailService` chứ không ở chỗ gọi.
212. **Con số trong message là số chủ cửa hàng CÓ email**, không phải số cửa hàng được chọn. Chủ đăng ký bằng số điện thoại thì không gửi được; bản cũ cũng lặng lẽ loại họ khỏi phép đếm y hệt. Giữ nguyên — chính con số đó là câu trả lời trung thực cho vận hành.
213. **`category` giữ free text**, không đóng thành enum: giá trị này đi thẳng vào subject line và console admin là client duy nhất, đóng set ở đây sẽ làm vỡ nó đúng hôm ai đó thêm chip thứ năm. `KNOWN_ANNOUNCEMENT_CATEGORIES` chỉ để tài liệu hoá 4 giá trị hiện có.
214. **Phân trang cả hai danh sách** (đã hỏi và chốt) — bản cũ là `limit(100)` cứng cho feed sự kiện và `find()` không giới hạn cho lịch sử announcement. Response đổi sang `{ data, pagination }`, frontend admin phải sửa theo.
215. Cả 7 handler cũ đều lặp lại `role === 'SUPER_ADMIN'` ở đầu — 7 bản sao của cùng 4 dòng. `AdminOnlyGuard` thay hết, để route thứ 8 không quên được.
216. **Bẫy dọn dẹp trong e2e**: announcement có `tenantId` null nhưng chạm tới tenant **qua bảng nối**, mà row bảng nối giữ FK tới tenant — nên phải xoá announcement **trước** khi xoá tenant, không thì FK chặn. Xoá notification thì cascade xuống `NotificationTargetTenant`.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **162/162** · **e2e 53/53**.

### 2026-08-27, phần 9 — trợ lý AI (batch 12), module cuối cùng

Port trọn `src/modules/ai`: 5 route giữ nguyên đường dẫn dưới `/ai`, 30 tool, vòng ReAct. **Đến đây mọi module của iKiotMS-BE đều đã có business logic thật.**

217. **AI từng là cửa sau đọc dữ liệu — đây là lỗ hổng nghiêm trọng nhất tìm thấy trong cả đợt review** (đã hỏi và chốt). Route cũ mở cho cả `TENANT_OWNER` lẫn `BRANCH_MANAGER`, nhưng `ai.tools.js` gọi mọi service với `mockUser = { tenantId, role: "TENANT_OWNER" }` cứng. Quản lý chi nhánh hỏi "doanh thu tháng này" là nhận **toàn bộ cửa hàng** — vòng qua đúng cái scope mà `/stats/overview` áp cho họ. Giờ truyền `AuthUser` thật xuống, và `AiToolsService.run` kiểm `can(user, resource, action)` trước khi dispatch.
218. **Bảng `TOOL_PERMISSIONS` fail OPEN nếu gõ sai tên resource** — `can()` không bao giờ được hỏi cho một resource không ai có, nên tool cứ thế chạy. Đã **mở rộng `scripts/check-permissions.js`** để soi cả bảng này, và nó bắt được ngay 3 tên sai lúc viết (`leave_requests`→`leaveRequests`, `working_schedules`→`schedules`, `inventories`→`inventory`). Đã thử cố tình gõ sai một dòng để xác nhận script bắt.
219. **Tool gọi vào module sở hữu câu hỏi, không tự query Prisma.** Doanh thu qua `StatsService`, đơn hàng qua `OrderService`. Đây là lý do module import 20 module khác — không phải cho gọn, mà vì bản sao thứ hai của các luật đó sẽ trôi đi và trả lời chủ cửa hàng bằng con số **lệch với chính dashboard của họ**. Không module nào import ngược lại nên đồ thị vẫn không có chu trình.
220. **Off-by-one ở vòng ReAct** (đã hỏi và chốt): bản cũ `while (loopCount < maxLoops)` rồi `if (loopCount >= maxLoops) throw`. Trả lời xong đúng ở vòng thứ 10 → `loopCount === 10 === maxLoops` → **vứt câu trả lời hợp lệ**, người dùng nhận "tôi đang bận". Giờ tách cờ "đã trả lời" khỏi bộ đếm; hết lượt và trả lời ở lượt cuối là hai kết cục khác nhau.
221. **`GeminiClient` là abstract class bind trong module** — đó là mối nối để unit test vòng ReAct bằng một model kịch bản, không cần mạng cũng không cần API key. 10 test: gọi tool → nạp kết quả → trả lời, nhiều tool trong một lượt, tool báo lỗi vẫn trả lời được, **trả lời ở lượt cuối vẫn tính là thành công**, cạn lượt thì ném, mỗi route arm đúng bộ tool của nó, và functionCall lạc ngoài route INTERNAL_DATA thì không được thực thi.
222. **Vì sao có bước phân loại ý định:** Gemini **không cho** arm custom function tools chung với Google Search grounding trong một request. Bước `classify()` tồn tại chỉ để chọn đúng một trong hai — không phải lựa chọn thiết kế. Nếu API sau này cho phép cả hai thì xoá luôn bước này. Phân loại lỗi thì fallback `GENERAL_LLM` và vẫn trả lời.
223. **Tham số từ model đi qua đúng DTO mà REST endpoint đang validate.** `plainToInstance` cấp default `page`/`limit`, `validate` chặn giá trị bịa; bị chặn thì trả về cho model dưới dạng tool error để nó đọc và thử lại — tốt hơn nhiều so với 500.
224. **Transcript là `jsonb`, chỉ lưu `{ role, parts: [{ text }] }`.** Tool call và kết quả của chúng ở lại trong đúng request sinh ra chúng, nên một cuộc hội thoại không phình lên vài MB JSON báo cáo. Đọc ra mà cột chứa thứ lạ thì degrade thành lịch sử rỗng chứ không làm hỏng request.
225. **Hội thoại thuộc về một người, không thuộc về cửa hàng** — mọi query scope theo `tenantId` **và** `userId`. Đồng nghiệp có `ai_chat:read` vẫn không đọc được thread của người khác. Hành vi cũ, giữ có chủ đích: người ta hỏi trợ lý những thứ không đưa vào hộp thư chung.
226. **Thiếu `GEMINI_API_KEY` trả 503, không phải câu fallback thân thiện.** Đó là việc của người vận hành phải sửa, chôn nó trong một bong bóng chat là cách để nó nằm im cả tuần. Mọi lỗi **khác** thì vẫn lưu transcript kèm câu fallback, để câu hỏi không bao giờ nằm trơ một mình — người dùng sẽ tưởng chưa gửi được và gõ lại.
227. **Tool báo cáo mặc định month-to-date**, không phải cửa sổ 30 ngày trượt của module stats. "Doanh thu tháng này" là câu người ta thật sự hỏi trợ lý; trả lời bằng 30 ngày trượt là sai theo kiểu không ai phát hiện ra. Bản cũ cũng vậy.
228. **Không test `/ai/chat` thật trong e2e** (đã hỏi và chốt) — một câu chat tốn 2–11 lượt gọi Gemini thật, chậm, tốn quota, và fail khi mạng hỏng hoặc model đổi tên. Vòng ReAct đã có unit test; e2e phủ phần còn lại: cổng quyền, scope theo người, và CRUD hội thoại.
229. Route cũ gate bằng `authorizeRoles("TENANT_OWNER", "BRANCH_MANAGER")` viết tay — hai vai trò cố định không còn tồn tại. Giờ gate theo resource `ai_chat`, `chat` dùng action **`create`** chứ không phải `read`: nó ghi hội thoại, gọi API tính tiền, và chạm vào dữ liệu cửa hàng.
230. Thêm dependency `@google/genai`. Tên model đọc từ `GEMINI_MODEL` **lúc gọi**, không phải lúc import — hằng số module được evaluate trước khi ConfigModule nạp `.env`, đọc ở đó thì vĩnh viễn chỉ thấy giá trị mặc định.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` sạch · unit **174/174** (thêm 12 test `ai-agent.service.spec.ts`) · **e2e 54/54**.

### 2026-08-27, phần 10 — review đối chiếu TOÀN BỘ source sau khi port xong (chưa sửa gì)

Rà lại từ đầu sau khi 12 batch đã xong: **220 route cũ** (trích cả kiểu `app.get(...)` lẫn kiểu bảng `{method, path}` trong `src/modules/*/index.js`) đối chiếu **237 route mới** (trích từ decorator Nest), cộng so từng service về guard, transaction, cron, middleware, cổng quyền. **Chỉ ghi nhận, chưa động vào code.**

#### Lỗ hổng / thiếu sót thật — 4 mục, cần sửa

231. **Hạn mức `maxUsers` của gói không còn được kiểm khi tạo nhân viên.** `StaffService.createStaff` cũ đếm nhân viên hiện có rồi chặn khi chạm `currentQuotaSnapshot.maxUsers`. `UserService.create` (`users.service.ts:129`) **không gọi `assertQuota`** — trong khi chi nhánh/kho (`location.service.ts:118`) và sản phẩm (`products.service.ts:95`) đều gọi. Hàm `assertQuota` đã hỗ trợ sẵn `'maxUsers'` (`subscriptions.service.ts:275`), chỉ thiếu chỗ gọi. Hệ quả: cửa hàng ở gói bất kỳ tạo được vô hạn tài khoản nhân viên — **thủng thẳng vào mô hình doanh thu**.
232. **Không kiểm trùng email khi tạo nhân viên.** `assertContactDetailsAreFree` chỉ được gọi từ `update` (`users.service.ts:188`), `create` chỉ kiểm số điện thoại. Schema **không có unique index** trên `User.email` lẫn `profileIdentificationId`, nên không có lưới đỡ ở tầng DB. Bản cũ `checkStaffUniqueness` kiểm cả ba ngay lúc tạo. Hệ quả: hai nhân viên cùng email tạo được, va chạm chỉ lộ ra khi ai đó sửa một trong hai. Cân nhắc sửa bằng cách gọi hàm sẵn có ở `create`, **và** thêm partial unique index (`WHERE status <> 'DELETED'`) cho chắc.
233. **Luồng onboarding hai bước đã biến mất, `POST /users/:id/account` gần như thành route chết.** Cũ: `POST /staff` tạo bản ghi **INACTIVE, chưa có mật khẩu** (`StaffDTO.js:9`) → `POST /staff/:id/account` mới đặt mật khẩu và kích hoạt. Mới: `CreateUserDto` **bắt buộc `password`**, tạo thẳng ACTIVE, nên `createAccount` (`users.service.ts:386`) luôn ném 409 với nhân viên vừa tạo; chỉ còn với tới được sau khi deactivate. Trạng thái "có tên trong sổ nhân sự nhưng chưa cấp login" **không còn tồn tại**. Có comment `deferred` trong `create-user.dto.ts` nhưng chưa ghi vào tài liệu nào — giờ ghi ở đây. Cần chốt: khôi phục luồng hai bước, hay chính thức bỏ nó và xoá `POST /users/:id/account`?
234. **Cron subscription thiếu timezone, hai cron kia thì có.** `subscription-cron.service.ts:42` dùng `@Cron(CronExpression.EVERY_DAY_AT_2AM)` không kèm `timeZone`, còn holiday và leave-request đều pin `Asia/Ho_Chi_Minh`. Trên host UTC (Render) nó chạy **09:00 giờ VN**. Bản cũ cũng thiếu, nhưng hai job kia đã được sửa trong lúc port còn job này thì không — **chính sự không nhất quán mới là bug**, và việc nó làm nhạy cảm với ngày (chuyển trạng thái hết hạn, đếm "còn N ngày" trong email nhắc).

#### Thay đổi làm vỡ frontend — chưa liệt kê ở đâu trước đây

235. **Đổi đường dẫn** (khác với cam kết "giữ nguyên đường dẫn cũ" ở các batch khác): toàn bộ `/staff/*` → `/users/*` (9 route); `GET /staff/roles` → `GET /roles`; và bỏ hậu tố `/delete` ở `DELETE /branches/:id/delete`, `/warehouses/:id/delete`, `/products/:id/delete`, `/products/items/:itemId/delete`.
236. **Đổi tên tham số phân trang**: `recordPerPage` → `limit`, ảnh hưởng **6 module** (attendances, leaveRequest, payroll, promotion, schedule, staff) — **cả tham số gửi lên lẫn khoá `pagination.recordPerPage` đọc xuống**. Kèm `keyword` → `search` ở danh sách nhân viên. Đây là thứ dễ làm vỡ nhiều màn hình nhất trong cả đợt; cần một bảng mapping gửi cho FE.
237. `_id` → `id` ở mọi response (hệ quả tất yếu của Mongo→Postgres). Ghi lại cho đủ.

#### Route cũ không port — đã xác minh vô hại, không cần làm gì

238. `GET /template` (route demo của scaffold, trả object tĩnh); `POST /notifications/test` (đẩy FCM thử, nhất quán với việc FCM chưa port); `express.static('/uploads')` trong `app.js` (bản cũ **đã dùng Cloudinary**, mount này phục vụ thư mục không ai ghi vào); `LeaveRequestService.updateLeaveRequest`/`deleteLeaveRequest` và `StaffService.getStaffById` (**code chết ở bản cũ** — không route nào gọi tới; bản mới còn thêm `GET /users/:id`).

#### Đã spot-check và khớp — không cần đụng

239. Vòng đời đơn hàng (transition + hoàn kho + CashFlow INCOME/EXPENSE, cộng conditional update làm khoá lạc quan); hạn mức lượt dùng khuyến mãi cả tổng lẫn theo khách, đều re-check **bên trong** transaction; guard stock-movement (trùng mặt hàng, giá nhập ≤ giá bán lẻ, nguồn ≠ đích, hạn mức công nợ NCC, `receivedQuantity`); chống vòng lặp cha-con của category; TTL token 1d/7d/15m và bcrypt cost 10; đủ 3 cron với đúng biểu thức; cửa sổ check-in và grace đi muộn; audit chỉ ghi method ghi dữ liệu ở cả hai bản; helmet + cors.
240. **Cổng quyền: không có route nào lỏng hơn bản cũ.** Đã kiểm riêng mấy chỗ nghi ngờ — `/roles/*` và `/audit-logs` là `OwnerOrAdminGuard`, `/admin/*` là `AdminOnlyGuard`, `POST /uploads` chỉ JWT đúng như cũ, leave-requests khớp từng dòng với `authorize()` cũ. (Script trích tự động ban đầu báo nhầm mấy route `/admin/*` và `/promotions/*` là không có gate — do nó không thấy `@UseGuards` đặt **trên** `@Controller`, và không thấy lời gọi `authorize()` dùng chung ở cuối vòng lặp bảng route bên bản cũ. Đã kiểm tay lại từng cái.)

### 2026-08-28 — sửa 4 lỗ hổng của review 27/8 phần 10

Đã sửa xong cả 231–234. Chi tiết từng mục:

241. **[231] Hạn mức seat.** `UserService.create` giờ gọi `assertQuota(tenantId, 'quotaSnapshotMaxUsers', …)` đếm STAFF chưa DELETED — cùng khuôn với chi nhánh/kho/sản phẩm. Kiểm **trước khi ghi row**, không phải lúc bật login: cửa hàng thêm được vô hạn bản ghi nhân viên rồi chỉ trả tiền cho người nào đăng nhập thì đó không còn là gói tính theo seat nữa. `UserModule` phải import thêm `SubscriptionModule`.
242. **[232] Trùng email khi tạo.** `create` giờ gọi chính hàm `assertContactDetailsAreFree` mà `update` vẫn dùng. Tham số `id` đổi thành `string | null` và lọc "trừ chính nó" chuyển sang spread — để `id: { not: null }` thì đọc thành "id khác null", tức mọi row: **đúng một cách tình cờ** ở nhánh create, và đó không phải thứ nên để lại.
243. **[232, phần DB] Thêm 2 partial unique index** (migration `20260828010000_user_unique_contact_details`): `users_one_email_per_tenant` và `users_one_identification_per_tenant`, đều `WHERE ... IS NOT NULL AND status <> 'DELETED'`. Lý do loại DELETED: xoá nhân viên là **ẩn danh hoá** chứ không xoá row, nên email cũ của người đã nghỉ không được chặn việc tuyển lại họ. Kiểm dữ liệu hiện có trước khi tạo index — 0 trùng ở cả hai cột. **`phoneNumber` cố ý không nằm trong index nào**: nó là handle đăng nhập toàn nền tảng, kiểm xuyên tenant trong service. Email đã được `@NormalizeEmail` hạ chữ thường + trim từ tầng DTO nên index cột thẳng là đủ, **không dùng `lower(email)`** — dùng expression sẽ khiến một đường ghi tương lai bỏ qua decorator vẫn lọt qua kiểm ở tầng app nhưng vẫn vấp index, tức là gộp đúng nửa tệ của cả hai cách.
244. **[233] Trả lại luồng hai bước, theo đúng bản cũ.** `CreateUserDto` **bỏ hẳn `password`**; `POST /users` tạo người ở trạng thái **INACTIVE, `password: null`**; `POST /users/:id/account` mới đặt mật khẩu và bật login. Đây là workflow thật: HR nhập nhân sự mới trước khi IT cấp quyền, một người có thể có paysheet + ca làm + số ngày phép trước khi đăng nhập lần đầu, và `deactivateAccount` xoá mật khẩu để đưa người ta về đúng trạng thái đó — trước khi sửa thì **không có đường quay lại trạng thái này**, và `createAccount` ném 409 với mọi nhân viên vừa tạo.
245. **[234] Cron subscription pin `Asia/Ho_Chi_Minh`.** Giữ nguyên literal giống hai cron kia thay vì tách hằng số dùng chung — nhưng lưu ý chuỗi `'Asia/Ho_Chi_Minh'` giờ **lặp ở 6 chỗ** (`email.service`, `cash-drawer.constants`, `stats.constants`, và 3 cron). Chính kiểu lặp này đẻ ra bug vừa sửa; gom về một hằng số dùng chung là việc nên làm, nhưng đụng 6 file nên để ngoài phạm vi lần này.

**Ảnh hưởng tới e2e** — 3 chỗ tạo nhân viên trong smoke suite phải tách làm hai bước. Thêm 3 nhóm assert mới: (a) tạo xong là INACTIVE, `password` null, **đăng nhập 401**, gọi `/account` rồi đăng nhập mới được 201; (b) tạm hạ `quotaSnapshotMaxUsers` xuống bằng số nhân viên đang có → `POST /users` trả **400** kèm chữ "giới hạn", rồi nâng lại vì phần sau của suite còn tuyển thêm 2 người; (c) đặt email cho nhân viên rồi tạo người khác bằng **email khác hoa thường** → **409**, chứng minh `@NormalizeEmail` chạy trước khi so.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` · `migrate status` sạch · unit **174/174** · **e2e 54/54**. Đã xác nhận 2 index thật sự tồn tại trong DB bằng `\di users_one*`.

**Còn lại từ review 27/8 phần 10**: mục 235–237 (đổi đường dẫn `/staff`→`/users`, bỏ hậu tố `/delete`, `recordPerPage`→`limit`, `keyword`→`search`, `_id`→`id`) — đây là **thay đổi hợp đồng API có chủ đích**, không phải bug; việc cần làm là gửi bảng mapping cho FE chứ không phải sửa code.

### 2026-08-28, phần 2 — rà sẵn sàng deploy

Soát lại toàn bộ dưới góc độ "đem lên server thì hỏng ở đâu". Code sạch (không còn `console.log`, không module nào chưa đăng ký, lint/tsc sạch) nhưng **tầng vận hành có 2 lỗi làm deploy chết ngay**, và mấy chỗ cấu hình dễ sập âm thầm.

246. **[BLOCKER] `generated/prisma` bị gitignore mà không có bước `prisma generate` nào khi build.** Checkout sạch + `pnpm install && pnpm build` là chết ở `Cannot find module '../../generated/prisma/client'`. Đã thêm `postinstall: prisma generate` **và** đưa vào luôn `build: prisma generate && nest build` — hai lớp, vì có nơi (Render) chạy build không qua install.
247. **[BLOCKER] `start:prod` trỏ sai file.** `nest build` xuất ra **`dist/src/main.js`** chứ không phải `dist/main.js`, vì `generated/prisma` nằm **ngoài** `src/` nên common root của TypeScript là thư mục dự án, cây output tụt xuống một cấp. Đã thử loại `prisma.config.ts` khỏi `tsconfig.build.json` — vẫn vậy, vì nguyên nhân thật là `generated/`. Sửa `start:prod` thành `node dist/src/main` và **ghi rõ trong CLAUDE.md**: đổi chỗ output của Prisma client thì phải kiểm lại dòng này. Đã **build rồi boot thật** artifact ở `NODE_ENV=production`, `/health` trả `{"status":"ok"}`.
248. **Không có fail-fast cho biến môi trường.** Thiếu `JWT_SECRET` thì app vẫn boot, vẫn trả healthcheck, được LB đánh dấu live, rồi mới chết ở **request đăng nhập đầu tiên** — lúc đó bản cũ đã bị gỡ. Thêm `validateEnv()` (`src/common/config/env.ts`) chạy **trước `NestFactory.create`**: chặn boot nếu thiếu `DATABASE_URL` hoặc secret; biến optional chỉ log warning kèm tính năng nào bị tắt, để log khởi động trả lời được câu "sao không gửi mail". Có 14 unit test (`env.spec.ts`).
249. **`ACCESS_TOKEN_SECRET` đã thành biến chết.** Bản cũ đọc `ACCESS_TOKEN_SECRET || JWT_SECRET`, bản mới chỉ đọc `JWT_SECRET`. **Môi trường production đang chạy đặt theo tên cũ** — deploy sẽ xanh rồi từ chối mọi lượt đăng nhập. Khôi phục đúng thứ tự ưu tiên cũ trong `accessTokenSecret()`, và cho **cả bên ký (`AuthModule`) lẫn bên verify (`JwtStrategy`) lẫn refresh token** đi qua đúng hàm đó, để không bao giờ lệch khoá nhau.
250. **CORS mở toang mặc định.** `CORS_ORIGIN` không đặt ⇒ `origin: true` (phản chiếu mọi origin) kèm `credentials: true` ⇒ **bất kỳ website nào cũng gọi API được bằng phiên của nạn nhân**. Đúng cho dev, là lỗ hổng trên production, và khoảng cách giữa hai bên đúng bằng một biến ai đó quên. `validateEnv` giờ **từ chối boot khi `NODE_ENV=production` mà CORS còn mở**. Đã kiểm chứng: boot thật với production + không CORS ⇒ chết đúng chỗ.
251. **Socket.IO và HTTP đọc hai biến CORS khác nhau** (`FRONTEND_URL` vs `CORS_ORIGIN`) — siết API xong thì socket vẫn mở. Gom về một hàm `corsOrigins()` dùng chung cho cả hai transport, `FRONTEND_URL` giữ làm fallback vì đó là tên đang có sẵn trên môi trường thật.
252. **Thiếu `app.enableShutdownHooks()`.** `PrismaService` và `RedisService` đều implement `OnModuleDestroy` nhưng **không bao giờ được gọi** — Nest chỉ nghe SIGTERM khi được yêu cầu. Render gửi SIGTERM mỗi lần deploy, nên đây là khác biệt giữa đóng pool tử tế và bỏ rơi nó.
253. **Healthcheck đổi đường dẫn mà không ai để ý**: bản cũ có `GET /health` trong `app.js`, bản mới chỉ có `GET /`. Uptime check / Render healthcheck đang trỏ `/health` sẽ fail ngay. Đã mount lại `/health` **song song** với `/`, không bỏ cái nào.
254. **Swagger đổi đường dẫn**: cũ `/api-docs`, mới `/docs`. Mount cả hai. Lưu ý: bản cũ **phơi Swagger công khai kể cả production**, bản mới giữ nguyên như vậy — đây là port trung thành, **không phải** quyết định bảo mật đã được cân nhắc. Muốn đóng thì đóng có chủ đích.
255. **Chưa từng có `.env.example`.** Đã viết, gom đủ 25+ biến, chia rõ *bắt buộc* / *bắt buộc trên production* / *optional kèm hậu quả khi thiếu*. Nhân tiện phát hiện `.env` local đang có **`JWT_SECRET` khai hai lần** — không sửa hộ vì đó là file chứa secret thật của máy bạn, tự dọn giúp.
256. **Cái bẫy tự vấp trong lúc sửa, đáng ghi lại:** đổi `JwtModule.registerAsync` sang `register()` đồng bộ làm **e2e chết 49/54**. Lý do là metadata của module được evaluate **lúc import file**, tức trước khi `ConfigModule` đọc `.env`, nên secret là chuỗi rỗng và mọi login 500. Đúng cùng loại bẫy với `geminiModel()` đã ghi ở mục 230. Giữ `registerAsync` và comment rõ vì sao. Hệ quả liên quan: `validateEnv()` chạy trước `NestFactory.create` nên cũng chưa có `.env` — phải `import 'dotenv/config'` ở **dòng đầu tiên** của `main.ts` và khai `dotenv` thành dependency tường minh.

**Kiểm chứng**: `tsc` · `lint` · `build` · `check-permissions` · `migrate status` sạch · unit **188/188** (thêm 14 test `env.spec.ts`) · **e2e 54/54**. Ngoài ra **boot thật artifact đã build** ở `NODE_ENV=production`: `/health` OK, `/docs` và `/api-docs` đều 200, warning env hiện đúng; và kiểm cả hai nhánh fail-fast (thiếu CORS trên production, thiếu secret) đều **chặn boot** thật.

**Việc cần làm phía hạ tầng khi deploy** (không phải việc của code):
- Đặt `NODE_ENV=production`, `CORS_ORIGIN`, `TRUST_PROXY=1`, `DATABASE_URL`, và secret (`ACCESS_TOKEN_SECRET` **hoặc** `JWT_SECRET`).
- Build command: `pnpm install && pnpm run build`. Start command: `pnpm run deploy:migrate && pnpm run start:prod`.
- Healthcheck trỏ `/health`.
- Cân nhắc có nên để `/docs` + `/api-docs` công khai không (mục 254).

## Việc CHƯA làm — đừng quên

**⇒ Ưu tiên tiếp theo: (1) cấu hình hạ tầng khi deploy — xem cuối mục 28/8 phần 2; (2) bảng mapping đổi tên API gửi FE (mục 235–237), nhất là `recordPerPage`→`limit` vì nó chạm 6 module và đổi cả khoá trong response.**

- **Đã hết module để port.** `Tenant` cố ý giữ CRUD admin-only; mọi model khác đều nằm trong `PORTED_MODELS`. `scripts/generate-modules.js` vẫn còn trong repo nhưng giờ không sinh ra gì nữa — giữ lại làm tài liệu về cách scaffold ban đầu, và **đừng chạy lại** trừ khi thêm model mới vào schema.
- **FCM push** — Socket.IO thật rồi (xem phần 2 ngày 17/8), nhưng push (app đóng/không mở) vẫn chưa — `UserFcmToken` đã thu thập được qua `POST /notifications/device-token` nhưng chưa có gì gửi tới token đó.
- **Notification/audit template**: domain nào port thật cũng phải tự thêm file `*.templates.ts`/`*.audit-template.ts` riêng theo đúng rule, không viết thẳng vào `NotificationService`/`AuditInterceptor`. Đã có cho subscription/order/inventory/stock-movement/supplier/staff/tenant/schedule/leave-request/payroll/ticket. AI chat không sinh notification nên không cần file template.
- **Lịch sử port** — đã xong: org/reference data 19/8 (mục 31–39), catalogue 25/8 (mục 58–73), Staff + StockMovement 25/8 phần 2 (mục 74–89), Order + Promotion + Customer 26/8 (mục 108–123), cash drawer 26/8 (132–141), Redis/phiên + cấu hình cửa hàng 26/8 phần 7, cash-flows/uploads/holidays 27/8 (batch 3–4), shift-template + working-schedule + quyền trưởng ca (batch 5), attendance (batch 6), leave request (batch 7), payroll (batch 8), ticket (batch 9), stats (batch 10), console thông báo vận hành (batch 11), trợ lý AI (batch 12 — module cuối). **Nhóm 1 (cấu hình nhân sự) và nhóm 3 (chấm công/nghỉ phép) — thứ tự chủ dự án chốt 25/8 để sau cùng — đều đã port xong ngày 27/8.**
- **`CashDrawerSession` chưa đối chiếu với `CashFlow`** — ca quầy ghi số đếm được (đầu ca, từng phiếu ca, cuối ca) nhưng chưa ai so nó với các dòng `CashFlow` mà bán hàng sinh ra trong cùng ngày/chi nhánh. Đó là phần báo cáo lệch quỹ, chưa port; xem mục 119 về việc vì sao dòng tiền thối không gắn `orderId`.
- **Còn một `follow-up migration` chưa làm**: `CHECK (num_nonnulls(branch_id, warehouse_id) = 1)` cho `inventories` — xem `prisma/schema.prisma` dòng ~617. Cùng loại với mục 132, và hiện chỉ có tầng ứng dụng chặn.
- ~~e2e chưa chạy~~ — **đã trả xong 25/8 phần 4**: e2e 17/17 trên Postgres thật, kèm smoke suite phủ 4 module port hôm nay. Nợ này mở từ 20/8 — Docker Desktop không chạy trên máy lúc làm nên chỉ có `test/di-check.e2e-spec.ts` (không cần DB) được xác minh. Chạy `docker compose up -d && pnpm run test:e2e` trước khi commit đợt này.
- ~~Còn nợ từ đợt 19/8~~ — đã trả xong cả hai: `Product.categoryName` (mục 62) và hạn mức công nợ NCC (mục 84), đều ngày 25/8.

## Trạng thái git (cập nhật 2026-08-26)

- `iKiot-BE`: remote `origin` = `https://github.com/iKiotMS/nestjs-ikiot.git`, nhánh `main`, **7 commit** — mới nhất `68cb643 add product, inventory and stock-movement`. Nghĩa là toàn bộ đợt 25/8 (product/inventory, staff/stock-movement, review, validator + smoke test) **đã được commit**.
- **Chưa commit: 49 mục** — toàn bộ ngày 26/8: order + promotion + customer (mục 108–123), review/sửa (124–131), cash drawer (132–137), review/sửa cash drawer (138–141). Gồm **2 migration mới**.
- `iKiotMS-BE`: remote `origin` = `https://github.com/iKiotMS/iKiotMS-BE.git`. Chỉ có `CLAUDE.md` chưa commit, **không có thay đổi code nào** — đúng quy ước một chiều.

Đừng tin đoạn này nếu nó lệch với `git log`/`git status` thật — cập nhật lại đoạn này (hoặc thêm mục ngày mới) mỗi khi commit/push để nó không lạc hậu.
