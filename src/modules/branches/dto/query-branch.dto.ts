import { QueryLocationDto } from '../../locations/dto/query-location.dto';

/** Branch list filters — identical to a warehouse's; see QueryLocationDto. */
export class QueryBranchDto extends QueryLocationDto {}
