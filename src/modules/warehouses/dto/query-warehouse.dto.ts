import { QueryLocationDto } from '../../locations/dto/query-location.dto';

/** Warehouse list filters — identical to a branch's; see QueryLocationDto. */
export class QueryWarehouseDto extends QueryLocationDto {}
